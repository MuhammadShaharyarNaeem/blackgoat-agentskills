# Changelog

All notable changes to the `blackgoat-agentskills` plugin are documented here.
Format follows [Keep a Changelog](https://keepachangelog.com/en/1.1.0/); versions follow [SemVer](https://semver.org/).

## [2.6.1] — 2026-09-08

The audit-fix release. The 2026-09-07 whole-plugin audit (nine lenses, 21 metrics) found 23 confirmed Blockers on the 2.6.0 tree; every one is closed here. Six gates that passed fabricated input now fail closed, the hook covers the shell writes and the bugfix route it missed, two dead pipeline routes are wired, four one-ended contracts in the 2.6.0 skills are documented at both ends, and the eval instrument is decontaminated.

### Migration notes — gate-contract changes
- **`check_quick_close.py` — `capture_command_mismatch`.** The capture's recorded `argv` must be the note's `- How verified:` command (token match, as `next_bugfix_route.py --red`). A note whose capture ran something else now fails; no waiver. `frozen_path_modified`'s message names the escape (narrow the glob, record why in `## Result`) instead of routing to bugfix.
- **`check_red_green.py --green-runs N`** requires N distinct process identities (sidecar `started`+`pid`): one capture cited N times, or N copies of it, is one run (`green_runs_not_distinct`). Identical bodies from distinct runs are accepted. Single-green invocations are unchanged.
- **`check_agent_report.py`** — every check line that cites a capture must carry the backticked command and it must match the capture's `argv` (`capture_command_mismatch`, never waived).
- **`check_openapi_diff.py`** — a compared schema carrying `oneOf`, `anyOf` or `not` is `unanalyzable_schema`, exit 2, no verdict, not waivable; `nullable: true → false` on a response is breaking (`nullable_removed`). The JSON gained `unanalyzable` and `warnings` arrays.
- **`check_handoff.py --advisory`** (opt-in) makes `<artifact>`/`<changed_skills>` optional for the two artifact-free handoffs the pipelines request (Forge's propose in `/bgpdd-learn`, Aria Mode 2 in `/bgpdd-build`); the ledger record carries `advisory: true`. `<status>` stays `COMPLETE|PARTIAL|BLOCKED`; the verification verdict belongs in the body.
- **`mark_milestone.py --reopen <milestone> --evidence <path> --reason "<text>" --ledger <path>`** removes a milestone's `[x]` (heading bytes unchanged) so `next_milestone.py` returns it; this is how `/bgpdd-shipping`'s exit-2 finding re-enters `/bgpdd-build`. **`--require-game-tape`** on `update_state.py` and `mark_milestone.py` accepts any `## bgpdd-<lane> — …` heading (widening only).
- **`record_run.py`** refuses a `--model` that names no tier (`model_unknown`, exit 2) instead of recording `tier: null`; `dep` is a producer for the verifier-below-producer check.
- **`check_tier1_provenance.py`** — a future-dated stamp fails (`stamp_date_future`); new `--verify-current` (consumer mode) fails on `tier1_drift` when a stamped sha is not the repo's HEAD, waivable with `--allow-drift "<reason>"`; plan, lite, build, verify and shipping now run it at entry.
- **The guard hook** denies shell writes to gate artifacts and frozen tests (`>>`, `tee`, `Set-Content`/`Out-File`, `python -c open()`, `sed -i`, `perl -pi`, `mv`/`rm`, `git checkout --`), arms the bugfix rules on the feature route, widens the git surface (`am`, `rebase`, `stash`, `notes`, `tag`, `gh pr merge`), no longer denies a lane's own merge after its commit gate passed with `--commit`, and does not arm the commit rule in a `bgpdd-verify` lane (its specs are committed by hand, step 4b).
- **BREAKING layout — bugfix feature route.** `{bugfix-root}` on the feature route is now `.docs/{project-name}/implementation/bugs/{bug-slug}/`; the two shared epic reports (`test-report.md`, `review-report.md`) stay at `{report-root}` = `.docs/{project-name}/implementation/`. An in-flight 2.6.0 bug keeps working where it is: `pipeline_driver.py` and the hook recognise the old shape. Do not move it mid-lane (the intake ledger hash would go `ledger_stale`); finish it, or move the folder and re-run intake and route.
- **`check_runtime_evidence.py` never parsed audit captures** — `dependency-upgrade-patterns` now cites them on an `**Audit capture:**` line under `evidence/upgrade/`; a 2.6.0 report citing them as `**Runtime evidence:**` fails that gate (it always did).
- **Plan tasks carry two new tags** owned by `planning-and-task-breakdown` § Contract-change tags: `[breaking: <kind> — <consumer impact>]` on any task authorising a breaking API-contract change (the builder returns the task unbuilt without it), and a flag-introducing task is written together with its removal task. Alex loads `api-contract-evolution` and `feature-flag-patterns` conditionally.

### Fixed
- Pipelines: shipping stamps `pipeline: bgpdd-shipping` at Step 0.2b, so its resume path and build's re-entry are reachable and the resume branch no longer re-applies `--require-go` to an exit verdict; shipping's exit-2 route reopens the affected milestone before handing to build; verify passes `check_acceptance_suite.py --emit-gate-args` and consumes `gate_args.argv` (the "no emitter exists" disclaimer was false); every agent handoff in bugfix, build, verify, lite, discovery and learn is validated with `check_handoff.py --since --ledger` and the gate is bound into bugfix's ledger-gates list; learn reads every lane root; discovery records a run log and names its Tier-1 consumers; build appends review sections, scopes `check_agent_report.py` by milestone, reads `artifacts.plan`; the branch-close protocol is owned once in `pipeline-skeleton.md`; quick admits any skill carrying a `## Quick card` (its spine said three skills while fourteen cards promised entry), keeps contract §1 timeout discipline and §2 halt triggers, and states the tree-hygiene rule when escalating to bugfix.
- Skills: `shipping-and-launch` cites `feature-flag-patterns` as the flag owner instead of restating a second lifecycle (the 2-week bound is labelled per #8); worker Escalate rows in the five 2.6.0 skills end at the Orchestrator (convention #6); `agents/mason.md` cited with a resolvable path; `pip audit` → `pip-audit`; argv-safe capture recipe in the privacy checklist; observability's intake fields match the bug-report template; `X-Correlation-Id`/`correlation_id` fixed at both ends; the one-question protocol has one owner.
- Personas: `base-persona.md` names `run_quiet.py --timeout`; Quinn's jobs row no longer fires on a plain relational write; Luna's Reviewer Directive covers every methodology she loads; `depends-on` completed for rex, luna, mason, nova; `source`/`date_added` parity.
- Docs: README version, agent model column (6 of 16 were wrong), the five 2.6.0 skills filed under Methodology skills; `pipeline-tools/SKILL.md` documents `--milestone` for `check_always_on.py`/`check_tier1_provenance.py`, `adr-citation`, the unkeyed-sidecar blast radius (one forger, seven gates), the authored nature of the run log, and its own size as a labelled convention-#1 divergence; `check_ledger.py`'s drift guard had silently dropped two writers (a missing comma).
- Evals: 35 pre-instrumentation trigger records archived out of the live `results.jsonl` (110 valid records remain); fixture-port pre-flight covers 15 ports with a drift self-test; the INFRA `rate limit` pattern no longer swallows a Cipher rate-limiting finding; zero-LLM records carry `case_sha256`; `weekly-check.ps1` wires `openapi-diff-adversarial`.

### Added
- CLAUDE.md convention #10 owns the `## Quick card` shape; all 17 cards conform (scripted check). `skills/agent-squad/references/authoring-agents-and-skills.md` holds the agent/skill authoring procedure (moved out of CLAUDE.md with the repo map and the heuristic list, which `agent-audit` owns).
- `runtime-evidence/references/environment-manifest.md` (the manifest's authoring half; spine 3,039 → 2,447 words, Quinn −620 and Vera −567 wake-up words); `dotnet-backend-patterns/references/solid-and-separation.md`; `check_always_on.py --require-row-agreement` (strict trim check, unarmed by default; the index states the description is canonical); `check_dependency_tables.py --help`.
- Self-tests: guard_action 42 → 88, check_openapi_diff 38 → 53, check_quick_close 46 → 55, check_red_green 31 → 38, check_agent_report 38 → 47, check_handoff 26 → 36, mark_milestone 36 → 50, update_state 44 → 47, check_tier1_provenance 16 → 27, record_run 31 → 35, check_ledger 21 → 23, pipeline_driver 59 → 61, check_always_on 18 → 23, check_dependency_tables 9 → 14. Zero-LLM suites: mechanical-pipeline 28 → 45, bugfix-gates-adversarial 18 → 21, openapi-diff-adversarial 25 → 34.

### Known, not fixed
- `check_commit_gate.py` still exits 0 on a wholly forged evidence base (capture, sidecar, ledger record and run log are all authored); the limit is now stated in `pipeline-tools/SKILL.md` § Limits. The five 2.6.0 spines sit at 1,536–1,605 words against a ~1,200 target; reaching it means deleting rules. Ten of fifteen agents still exceed 2,500 wake-up words. The 36 eval cases with no valid record (quick-lane, the three 2.6.0 contract cases, four pressure cases, 22 trigger cases) remain unrun by decision; the gate changes above are verified by self-tests and the zero-LLM suites only until they run.

## [2.6.0] — 2026-09-07

The daily-driver and coverage release: the quick lane gets cheaper and stack-aware, every methodology carries a Quick card for inline use, and five new skills cover the work that happens between features and in production.

### Migration notes
- **Quick lane defaults come from `detect_stack.py`.** Phase 0 now proposes the check command and the frozen test globs from the detected stack; you confirm or replace the command, never the lane silently. `check_quick_close.py --frozen` accepts globs; metacharacter-free values behave exactly as before.
- **`check_coverage.py --design` gains `adr-citation`.** A Divergence & Supersession Register row that cites a known FR/NFR must also cite the ADR that authorised it (`ADR-NNNN` or an `adr/NNNN-` path). Design documents written under 2.5.0 with register rows fail this lint until an ADR is written and cited.
- **`check_openapi_diff.py` is bound into build's commit gate for `[API]` milestones with an OpenAPI document and into shipping Step 3.** A breaking change needs `--allow-breaking "<reason>"`, recorded in the chained ledger.
- **New persona dependency rows are all conditional**; no agent's Always wake-up load changed.

### Added
- **Quick lane usability**: the mandatory read is trimmed to the contract's Runtime Neutrality and §3 plus the skeleton (labelled refinement; roughly 3,500 fewer words per run); stack-driven `suggested_check_commands` and `test_path_globs` in `detect_stack.py` (18 → 26 self-tests); glob-aware `--frozen` in `check_quick_close.py` (39 → 46); the driver prints the suggested command and the detected globs (52 → 59); the "test went red while I changed it stays in quick, a pre-existing defect is bugfix" rule in quick and `/bg`; quick's escalation seeds the bug report from the note.
- **Quick cards** in the eight directly-invocable methodology skills (≤ 150 words) and four stack skills (≤ 100): the five contract rules that matter at ≤ 3 files, each citing its section, plus the inline mapping (note = brief, capture = artifact, Result bullet = handoff).
- **New skills**: `dependency-upgrade-patterns`, `feature-flag-patterns`, `jobs-and-messaging-patterns`, `observability-and-diagnosis`, `api-contract-evolution`, each with a Worker Execution Contract, Quick card, Direct invocation, references, conditional persona rows, and a contract eval case.
- **`check_openapi_diff.py`** (38 self-tests): removed paths, operations, status codes and fields; type changes; new required request fields; enum narrowing; `$ref` resolved one level; JSON or the JSON-compatible YAML subset (everything else refused, never guessed). `evals/contract/openapi-diff-adversarial` (25 zero-LLM steps) registered in the harness.
- **Architecture decision records**: `blackgoat-research/references/adr-template.md`; every ≥ 2-option design decision gets an ADR; register rows cite it; `check_coverage.py` `adr-citation` lint (126 → 132 self-tests).
- **Data-privacy checklist** in Cipher's check-line grammar; Cipher's dependency row and security-and-hardening pointer.
- **Doubt-driven development** gains three job-semantics attacks (duplicate delivery, out-of-order delivery, crash between effect and ack).
- **Eval cases** (authored, self-checked on held and caved trees, not yet run): `quick-lane`, `dependency-upgrade-contract`, `jobs-idempotency-contract`. Harness pre-flight also checks port 5193.

### Changed
- README skill catalog lists 43 skills; bug-report template notes pre-fill from a quick note or from telemetry.

### Checked, not added
- Accessibility is already a Nova-side contract in `ui-design-patterns` plus Vera's shipping depth; CI YAML authoring is covered by `cloud-deploy-patterns`; race/concurrency *diagnosis* is partial (prevention and adversarial review exist, no diagnostic method) — a candidate for a later skill.

## [2.5.0] — 2026-09-07

The hardening release. Restraint moves from "the model should not" to "the model cannot": a PreToolUse hook denies the actions the gates exist to catch, a driver emits the next mandatory action instead of the Orchestrator recalling it, every agent handoff is validated by a script, and the gate ledger is a hash chain.

### Migration notes — read before upgrading a project with in-flight `.docs/` artifacts

- **The ledger is chained.** Every record now carries `prev` and `self`. Existing ledgers keep working: legacy records are tolerated before the first chained one, and a chained ledger may never revert to unchained. `check_commit_gate.py --require-ledger-gates` and `mark_milestone.py --require-gates` verify the chain first and fail with `ledger_chain_broken`; a ledger someone edited by hand will not pass again. Accepted limit: a tail rewritten consistently is undetectable; a single edited or inserted record is now visible.
- **Hooks are active in Claude Code.** `guard_action.py` denies `git commit|merge|cherry-pick|revert` from the shell while a lane is active (the gates commit through their own subprocess and are unaffected), edits to existing test files during a bugfix before its commit gate passes, delegation while a bug report lacks an intake PASS, and any direct write to `gates.jsonl`, `orchestrator-state.json`, `run-log.jsonl` or a `*.meta.json`. A lane counts as active from its artifacts within a 12-hour window. Outside a lane nothing changes. Cursor cannot enforce these; the rules are stated in its runtime contract.
- **Build's commit gate and bugfix's Phase 5 gate require the run log**: `--require-run-log … --require-agents quinn,luna,<builder>` fails the commit when any named agent has no delegation record with a model for that unit. A milestone whose delegations were never recorded under 2.4.0 cannot be committed under 2.5.0 until `record_run.py` lines are added for them.
- **Milestone closure requires the game tape**: `mark_milestone.py --require-game-tape` and `update_state.py --require-game-tape` refuse the write when the milestone's section is missing, has fewer than 3 or more than 6 bullets, has no fenced output, or no telemetry line.
- **Shipping's ship decision binds coverage and acceptance**: `check_ship_decision.py --require-ledger-gates check_coverage.py,check_acceptance_suite.py` (lite epics omit the acceptance suite). Run Step 3.5's coverage gate first if the ledger has no coverage PASS.
- **`record_run.py` refuses a verifier recorded below its producer's tier** in the same unit unless `--allow-tier-inversion "<reason>"` is recorded.
- **Discovery's Tier-1 stamp has a grammar**: `check_tier1_provenance.py` requires a date and a 40-hex sha per repo in the header of `context.md` and each `<feature>/overview.md`, and the sha must resolve. Existing Tier-1 files without a stamp fail with `stamp_missing`; add the header line.

### Added
- `guard_action.py` (42 self-tests) and three `PreToolUse` entries in `hooks/hooks.json`; proven end to end with a headless session that was denied a hand commit inside an active lane and allowed one outside.
- `pipeline_driver.py` (52): the next mandatory action for the bugfix and quick lanes, exit 1 when the current phase's gate has not passed, exit 3 on completion; wired into both spines ("phase transitions are emitted, not recalled").
- `check_handoff.py` (26): required elements per persona, path existence, changed files within the diff, status enum, consumers grammar, PASS beside NOT VERIFIED; orchestrator-contract §1 validates every handoff before acting on it.
- `check_ledger.py` (21): chain verifier; drift guards assert every writer chains byte-identically.
- `check_always_on.py` (18), `check_tier1_provenance.py` (16), `check_runtime_recipe.py` (16); discovery Phase 1, 4 and 4b wired.
- `check_ship_decision.py --require-ledger-gates`; `--require-game-tape` on `mark_milestone.py` and `update_state.py`; `check_commit_gate.py --require-run-log/--require-agents`; `record_run.py --allow-tier-inversion`.
- CI: ubuntu + windows matrix running both lints, every pipeline-tools self-test, both zero-LLM cases, `check_always_on.py`, the harness self-test and a parse of every PowerShell file.
- `mechanical-pipeline` steps 13–16 (28/28).

### Changed
- `--ledger` on the `--add-blocker` calls in shipping Step 3, build Phase 0 and plan.
- `pipeline-tools/SKILL.md` description names every script in 802 chars; sections for all seven new scripts.

### Not adopted
- LangGraph. It replaces the Claude Code host; the driver plus hooks give the deterministic control flow it would have provided, inside the runtime the plugin already lives in. If headless pipeline runs are ever needed, the Agent SDK with `pipeline_driver.py` is the path.

## [2.4.0] — 2026-09-07

The audit release. A 21-metric fan-out audit of 2.3.0 found six Blocker metrics; every one is closed here, and two of the fixes change gate contracts.

### Migration notes — read before upgrading a project with in-flight `.docs/` artifacts

- **Cipher and Vera check lines now cite captures.** `check_agent_report.py` requires every executed `PASS`/`FAIL` line to end with `— capture: evidence/<dir>/<file>.md`, and the cited capture's sidecar must exist, hash-match, and carry the same exit code the line claims (`check_uncaptured`, `check_capture_disagrees`). A report authored under 2.3.0 fails closed; re-run each check through `run_quiet.py --capture` and cite it. `BLOCKED` / `NOT RUN` lines are exempt.
- **Capture sidecars are cross-checked against the capture body.** `check_runtime_evidence.py`, `check_red_green.py`, `check_quick_close.py` and `check_ship_decision.py` now require the hash-protected body's `- Exit code:` and `- Captured:` lines to agree with the sidecar (`sidecar_body_disagrees`). A capture written by a pre-2.1 `run_quiet.py` without those header lines fails with `capture_header_missing`; re-take it.
- **`mark_milestone.py --require-gates` needs `--ledger`** and exits 2 without it. A bare invocation used to append `[x]` unchecked.
- **`bgpdd-bugfix` feature route**: the fix is made on the epic's existing branch, read from the state file; the lane never writes `branch`. It applies only while the epic is in flight (state exists and its branch exists unmerged); a bug in a shipped epic takes the standalone route. Quinn and Luna APPEND their sections to the epic's `test-report.md` and `review-report.md`.
- **`bgpdd-plan` Phase 3.6** now sets `--set-pipeline bgpdd-plan` on both `--init` calls. A run interrupted between 3.6 and Phase 4 under 2.3.0 left `pipeline: ""`; set it by hand with `update_state.py --set-pipeline bgpdd-plan` before resuming.
- **Eval harness is version 4.** INFRA runs are auto-classified, retried once, and written to `results-invalid-infra-<date>.jsonl` with `pass: null`; a batch pre-flight probes the CLI and the fixture ports; trigger records carry `skills_invoked`. The live `results.jsonl` holds only flat records; the 160 legacy array-shaped rows moved to `results-legacy-array-shape-2026-08.jsonl`.

### Added
- `<consumers>` handoff element defined in `agents/mason.md` and `agents/nova.md` (`path::symbol`, when the brief asks) — the bugfix lane required it, nothing defined it.
- `/bg` state rows for "execute the existing plan" → `bgpdd-build` and "map this codebase" → `bgpdd-discovery`, above the three questions.
- Per-delegation `record_run.py` lines (mandatory `--model`) in `bgpdd-plan`, `bgpdd-lite`, `bgpdd-verify` and `bgpdd-shipping`, so shipping's whole-epic roll-up is complete.
- `bgpdd-build` sub-contracts: `references/build-phase0.md`, `build-gate-ladders.md`, `build-phase5-gates.md`, `build-phase6.md`, each fronted by a ≤ 120-word spine stub with a MANDATORY read line naming every HALT it holds.
- `evals/eval_record.py`; `--record` on both zero-LLM cases, run at the start of every confirmed contract batch. `expected_chain` for two-hop trigger cases.
- `next_bugfix_route.py` scopes its intake lookup by `--milestone`.

### Changed
- `agents/cipher.md`, `agents/vera.md`: `model: opus` (they gate opus builders; Metric 14).
- `agents/luna.md`: report path "unless the brief names another path — the brief wins" (standalone bugfix collision).
- Persona frontmatter: `phase:` names every lane the agent runs in; `depends-on` complete (aria + scout, echo; alex + echo; luna + quinn).
- `bgpdd-build` 7,227 → 5,080 words; `bgpdd-plan` 3,994 → 3,455 (`references/plan-rationale.md`); `bgpdd-lite` 2,526 → 2,205 (`references/lite-rationale.md`); `bgpdd-learn` reads the skeleton. Script/flag/path/exit-route inventories set-identical across spine + references.
- `pipeline-skeleton.md`: Game Tape default is Contract §4's every-state-persistence; inventory lists the three labelled refinements and the two no-tape lanes.
- `pipeline-tools/SKILL.md` description ≤ 1,024 chars (was 1,169); `already_committed` documented.
- README skill catalog lists all 38 skills.

### Fixed
- Bugfix Phase 4 "writes" → "appends" (a truncating write destroyed FR-traced evidence build and shipping gate on).
- `bgpdd-verify` gate flags copied by eye are pasted into the game tape beside the command (interim, labelled, until an emitter exists).
- `mechanical-pipeline` fixture: capture and sidecar timestamps disagreed by 223 days (exposed by the new cross-check).

### Known, not fixed
- Binding `check_coverage.py` / `check_acceptance_suite.py` into build's per-milestone commit gate deadlocks from milestone 2 (their PASS hashes `plan.md`, which `mark_milestone` mutates); the right home is the epic-scoped ship decision, which has no `--require-ledger-gates` yet.
- Wake-up weight: eight agents above ~2,500 words (Quinn 6,254). Quinn §3/§4 and Luna §2/§4 procedural catalogues still live in the personas; no methodology owner line covers them yet.
- The launch checklist is stated in four files; both methodology skills cite the authoritative `references/*-checklist.md`.
- Pressure suite and the 22 unmeasured trigger cases remain unrun (deferred by the user).

## [2.3.0] — 2026-09-04

The daily-driver wave: the plugin now serves ordinary work, not only epics.

### Added
- **`/bg` — the front door** (`skills/bg/SKILL.md`). Classifies an everyday ask with three questions (a reproduction of wrong behaviour → `bgpdd-bugfix`; new capability, schema or contract, or unknown spec → `bgpdd-plan`, discovery first when the codebase is unmapped; ≤ 3 files with no external dependents → `bgpdd-quick`; else spec known and pattern established → `bgpdd-lite`), announces the classification and invokes exactly one lane. Named lane wins; ambiguity costs exactly one question; lanes escalate upward only. Never does the work itself.
- **`/bgpdd-quick` — the daily-driver lane** (`skills/bgpdd-quick/SKILL.md`, `references/quick-rationale.md`). One contained change of ≤ 3 files, main session only, no delegation, no epic: a three-line `note.md` (What / Where / How verified), the check run through `run_quiet.py --capture`, and `check_quick_close.py` as the single gate — note complete and matching the declared files, capture sidecar-backed and newer than every changed file, no undeclared tree changes, frozen tests unedited (adding a test is allowed), size bound with the escalation lane named, commit of exactly the declared files. Labelled divergences: the Orchestrator is the worker (Contract §3), no state file or run log (Contract §4), one-bullet game tape in `note.md`, round bound 1, no size waiver.
- **Always-on index via a session-start hook** (`hooks/hooks.json`, `hooks/run-hook.cmd`, `hooks/session-start`, `skills/agent-squad/always-on.md`). Injected on `startup|clear|compact`: one line per lane taken from each lane's own description, plus the four rules that bind outside any lane, each pointing at its owner. Runtime-neutral content held once; Cursor reads the same file via `rules/cursor-runtime.mdc`. Proven end to end with a headless `claude -p --plugin-dir` session.
- **Direct invocation of the worker methodologies.** TDD, debugging-and-error-recovery, code-review-and-quality, code-simplification, source-driven-development, doubt-driven-development, performance-optimization and security-and-hardening can be asked for on named files outside any pipeline; the Orchestrator applies the Worker Execution Contract inline, no delegation, and routes anything over three files or touching shared behaviour through `/bg`. agent-audit Metric 12 carries the sanctioned exception.
- **`review_package.py`** (`pipeline-tools`, 19 self-tests): writes commits + stat + unified diff for a range or the uncommitted worktree (`--head WORKTREE`) with a run_quiet-shaped sidecar. `bgpdd-build` Phase 3 and `bgpdd-bugfix` Phase 5 generate it before briefing Luna and regenerate it per remediation round — "review the remediation diff" is now an artifact she opens, not a path list she reads around.
- **`record_run.py --model` is mandatory for `--event delegation`** (exit 2 without it; `--from-json` may supply it). Alex's plan task block gains an optional `Model tier` recommendation; agent-audit Metric 14 still owns the verifier-tier rule.
- **Branch-close step** in `bgpdd-bugfix` (Phase 5 step 9) and `bgpdd-lite`: base branch read from the repo, exactly three options (merge locally / publish and open a pull request / keep), typed branch name to discard, post-merge suite captured with `run_quiet.py`, worktree cleanup. Labelled divergence from shipping Step 4.5/6.6.
- **Evals**: 9 trigger cases for the new lanes (4 quick, 2 bugfix, 1 lite, 1 plan, 2 via `/bg`); README documents that a `/bg` case grades only the first hop until the judge records the invocation chain. `weekly-check.ps1` maps the quick lane, the skeleton and the new gates.

### Changed
- `bgpdd-bugfix` Phase 3: **the builder does NOT commit** — the commit is Phase 5's gate (from eval run 1, where Mason committed his own fix 17 minutes before the gate ran).
- `check_commit_gate.py --commit` (79 self-tests) refuses with `already_committed` when no declared file differs from HEAD: a fix committed outside the gate can no longer be passed around it. A partial checkpoint commit with other declared files still dirty is unaffected.
- `agents/luna.md` reviews the packaged diff when one is named; `agents/alex.md` may recommend a model tier per task.

### Fixed
- `evals/contract/bgpdd-bugfix-lane` fixture ignores `handoff.txt` (the harness's own artifact tripped `--verify-tree`) and its Command passes `--allowedTools` (headless runs deny write-effect commands without it). Run 1 of the lane graded 9/11 in 36 minutes; both failures traced to these.

## [2.2.1] — 2026-09-04

### Migration notes — read before upgrading a project with in-flight `.docs/` artifacts

- **`/bgpdd-bugfix` is a new lane.** A bug started under 2.1.0 has no `bug-report.md`, no RED capture and no route record, so Phase 2's `next_bugfix_route.py` refuses to route it (`intake_unbacked`). Re-enter at Phase 0: write the report from `skills/bgpdd-bugfix/references/bug-report-template.md`, gate it, and let Quinn take the RED before the fix continues. On a feature route the lane now uses the epic's own `orchestrator-state.json` and never changes its `pipeline`; a second state file inside `implementation/` from an interrupted 2.1.0 bugfix should be deleted.
- **`check_runtime_evidence.py` ledger keys changed.** Cited captures are now keyed by their cwd-relative resolved path instead of the citation string, so `check_commit_gate.py --require-ledger-gates check_runtime_evidence.py` can re-hash them. A 2.1.0 ledger entry whose key was a report-relative citation will read as stale at the next commit gate; re-run the runtime-evidence gate once for that milestone.
- **Every pipeline now reads `skills/agent-squad/pipeline-skeleton.md`** right after the Orchestrator Contract. Nothing changes in behaviour; the boilerplate each pipeline used to carry (path resolution, error recovery, chain of thought, game tape) lives there once. A runtime that vendors the `skills/` tree must include the new file.
- **`check_commit_gate.py --max-changed-files`** is opt-in; existing build invocations without it behave exactly as before (self-test `test_pipeline_value_never_changes_the_verdict` also guards that the state file's `pipeline` value never affects a verdict).

### Added
- **`bgpdd-bugfix` rewrite** (`skills/bgpdd-bugfix/SKILL.md`, `references/bug-report-template.md`, `references/rca-template.md`, `references/bugfix-rationale.md`): six phases on written evidence — lint-gated intake, Quinn-owned RED capture, Orchestrator RCA with a hypothesis ledger and a scripted FAST/FULL/PLAN route, fix by Mason and/or Nova who may not touch the RED, same-command GREEN plus the full suite, fresh Luna, bounded commit gate, Tier-1 prevent write-back, next command named. FAST and FULL differ only in user check-ins; neither skips RED, GREEN, Luna or the commit gate.
- **`check_bugfix_intake.py`** (33 self-tests): sections present and non-placeholder, a backtick-wrapped runnable `- Command:` or at least two numbered steps, `Surface` / `Runtime observable` / `Regression` enums, last-known-good on a regression, ledger record with the report's hash.
- **`check_red_green.py`** (24): both captures sidecar-backed, identical `argv`, RED exit non-zero, GREEN exit zero and newer, `--green-runs N` for flaky bugs (4 of 5 is not fixed), capture-hash tamper check.
- **`next_bugfix_route.py`** (49): refuses without an intake PASS whose hash still matches the report; `--red` ties the RED sidecar's `argv` to the report's command by shell-token comparison; prints FAST / FULL / PLAN with reasons.
- **`check_commit_gate.py --max-changed-files N --waiver <rca.md>`** (76): fix-size bound, waivable only by a non-empty, non-placeholder `## Size waiver` section — the user's recorded decision, deliberately hand-typed.
- **`skills/agent-squad/pipeline-skeleton.md`**: the boilerplate shared by every `bgpdd-*` pipeline, held once (path resolution, error recovery pointer, upgraded chain of thought, game-tape default cadence). Pipelines refine it only where labelled (convention #8).
- **Evals**: `contract/bgpdd-bugfix-lane` — the first case that measures a whole pipeline; eleven criteria re-derived from disk (intake hash, Quinn's RED before any builder, RED/GREEN sidecar agreement, frozen tests untouched, FAST mechanically implied, executed disproof in the RCA, exactly one gated commit, defect gone on the wire, prevent step appended). `contract/bugfix-gates-adversarial/run.py` — zero-LLM walk of the four bugfix gates over fabricated inputs (17/17). `weekly-check.ps1` maps the lane, the skeleton and the new gates to the case.

### Changed
- **`bgpdd-build`** distilled 8,345 → 7,276 words; rationale, observed-failure narratives and the few-shot example moved to `references/build-rationale.md`; the commit-gate bullet is an eight-step ordered list with every flag intact; script/flag/HALT/exit-route sets identical.
- **`bgpdd-shipping`** distilled 6,643 → 5,214 words (`references/shipping-rationale.md`, `references/shipping-delegation-briefs.md` — the three delegation prompts, pasted verbatim at each step); **`bgpdd-discovery`** 2,733 → 2,482 (`references/discovery-rationale.md`; provenance-stamp and runtime-environment rules kept at full strength); **`bgpdd-verify`** states "Quinn never authors what she is graded against" once with four corollaries and gains the bidirectional contract with `/bgpdd-bugfix`. `bgpdd-plan` and `bgpdd-lite` swap their boilerplate for the skeleton read; no other change.
- **`agents/echo.md`**: `manual-testing.md` now names both sanctioned non-Echo writers (shipping Step 6.4 and the bugfix prevent step).
- **`bgpdd-bugfix` reproduction command is an exit-code oracle**: `curl --fail` qualifies only when the correct response is 2xx; for a bug whose correct behaviour is itself an error status the command is a one-line status assertion, and Quinn takes a separate plain `curl` capture for `check_runtime_evidence.py`.

### Fixed
- **`check_runtime_evidence.py`**: ledger `inputs` keyed by a re-hashable path (a report-relative citation used to block the commit gate as `ledger_stale`, in `bgpdd-build` too); an unquoted executable path containing spaces (`C:\Program Files\…\curl.EXE`) is now recognised as its client instead of being rejected as `program`.
- **`bgpdd-build` hydration** no longer advertises a `bgpdd-bugfix` re-entry that nothing could reach.
- **Eval harness notes**: headless `claude -p` denies every write-effect command unless `--allowedTools` names the tools; the bugfix-lane case passes them, and the two aborted runs are archived as INFRA in `results-invalid-infra-2026-09-03.jsonl`.

## [2.1.0] — 2026-09-03

### Migration notes — read before upgrading a project with in-flight `.docs/` artifacts

Several gates now fail closed on artifacts that passed under 2.0.0. Nothing changes for work started after the upgrade; an epic mid-build needs one of the following per item.

- **Review reports**: `**Verdict:**` must appear before any `###` subheading inside its `## Review:` section. A report written to the old bottom-of-section template reads as no verdict. Move the line up.
- **Runtime captures**: every capture cited by `**Runtime evidence:**` must have the `<capture>.meta.json` sidecar `run_quiet.py --capture` now writes, with a matching hash and a zero probe exit. Regenerate old captures, or pass `--allow-missing-sidecar` to `check_runtime_evidence.py` / `check_commit_gate.py` for that milestone only (absence is waived; a mismatch or failed probe never is).
- **Probe commands** must be a real client (`curl`, `httpie`, `newman`, `psql`, …) or carry `[probe-exempt: <reason>]` on the probe line.
- **Coverage ledgers**: a `PASS` line must cite an exit code, a `file::test` reference, or a capture path; `- FR-1: PASS — done` is now `UNEVIDENCED` and uncovered.
- **Rendered evidence** must be a non-empty image with valid magic bytes, newer than the changed files.
- **Milestone closure**: `[x]` is written only by `mark_milestone.py --require-commit --ledger`; a hand-typed marker is refused by the commit gate on the next milestone.
- **Blockers**: legacy string entries still work (read as unscoped, Critical) and are never rewritten; new entries are structured and scoped by milestone.
- **Ship decisions at shipping Step 3** now require a rehearsed rollback line and a three-metric baseline section; prep decisions at build Phase 5 and shipping Step 0.4 do not.
- **Eval results**: every trigger-suite record before harness version 3 measured whether a skill was *mentioned*, not whether it was *invoked*, and must not be read as routing accuracy.

Only the Scout frontmatter break in 2.0.0 came from the distillation wave; `bgpdd-verify` and `doubt-driven-development` had never registered since they shipped.


A hardening wave over the 2026-09-02 audit. Its through-line is convention #9: every rule in here that an agent or the Orchestrator was previously asked to honor voluntarily — a gate that must actually have run, a capture that must actually have been observed, a completion marker that must actually be backed — is now an artifact something has to produce, hash, or refuse.

### Added
- **The gate ledger** (`pipeline-tools`, `orchestrator-contract` §4). Every gate in the family accepts `--ledger <path>` and appends exactly one JSON line per run on **every** exit path: timestamp, gate name, argv, milestone, a `{path: sha256}` map of every input it read, verdict, exit code. Writing it is best-effort and never changes the writing gate's own verdict — it is an audit trail for LATER gates. The failure it answers: nothing downstream could tell a gate that fired and passed from a gate that was never run, so "the gate passed" was a claim in prose with no artifact behind it, and the shipping game tape's fired-vs-rubber-stamped column was written from memory. Every pipeline gate invocation now carries `--ledger .docs/{project-name}/implementation/gates.jsonl`.
- **`check_commit_gate.py --require-ledger-gates <name>[,<name>...]`**: for each named gate, the LATEST ledger entry scoped to this milestone must record `PASS` **and** every input it hashed must still hash the same on disk (`ledger_missing` / `ledger_failed` / `ledger_stale` in `ledger_gate_problems`). Existence of a sibling gate was never the question; whether it ran, passed, and passed over *these* bytes was. `bgpdd-build`'s commit gate now requires `check_runtime_evidence.py`, plus `check_agent_report.py` on `[SEC]` milestones.
- **`mark_milestone.py`** (`pipeline-tools`): the write side of the `[x]` completion convention `next_milestone.py` reads. Until it existed a milestone could be marked done by typing three characters — no commit, no gate, no evidence. It appends the marker only on a whole-token title match (`Milestone 1` never marks `Milestone 10`), refuses with a named problem code (`milestone_not_found`, `ambiguous_milestone`, `already_complete`, `no_commit`, `ledger_missing`, `ledger_failed`), and optionally demands a commit naming the milestone (`--require-commit`) and a PASS ledger entry (`--require-gates`, default `check_commit_gate.py`). `bgpdd-build` now closes every milestone through it; a hand-typed `[x]` is no longer sanctioned.
- **The provenance sidecar** (`run_quiet.py --capture`): every capture now gets a machine-owned `<capture>.meta.json` beside it recording `argv`, `cwd`, `host`, `pid`, `started`/`finished`, the real `exit_code`, `body_sha256`, and `capture_sha256` over the finished file's bytes. Owning the capture's load-bearing *fields* stopped an agent writing `Exit code: 0` over a failure; it did nothing about a capture authored end-to-end, because a hand-typed artifact and an observed one are the same bytes. `check_runtime_evidence.py` now requires the sidecar and rejects `sidecar_missing`, `sidecar_hash_mismatch` (the artifact was edited after it was recorded) and `probe_failed_exit` — **a probe whose client exited non-zero observed nothing, regardless of what its body says**; a `curl` refused a connection exits 7 and prints a well-formed nothing, and that capture used to pass every check. `--allow-missing-sidecar` waives absence only, loudly, for legacy captures.
- **The probe client allowlist** (`check_runtime_evidence.py`): the first executable token of `Probe command:` — after env assignments, `timeout`, and a `run_quiet.py --` wrapper are peeled — must be a real client (`curl`, `wget`, `httpie`, `playwright`, `psql`, `grpcurl`, `Invoke-RestMethod`, …), else `probe_not_client`. The blocklist form let `run_quiet.py --capture -- python -c "print('HTTP/1.1 200 OK')…"` produce a fully conforming capture *with a genuine sidecar*: the provenance was real, the client was an interpreter printing a transcript. The escape hatch is `[probe-exempt: <reason>]` on the probe line — it waives the allowlist and nothing else, and an unlisted client is treated as unproven rather than hostile.
- **`next_milestone.py --emit-gate-args`**: parses the next milestone's own `RUNTIME PROBE:` line into `{surface, expect_status, require_keys, forbid_hosts}` so the Orchestrator stops re-deriving the runtime gate's arguments by eye. Deliberately conservative — a field comes back null with a warning when the probe lines disagree, because a wrong assertion silently weakens the gate it feeds. `--ledger` additionally reports `unbacked_complete`: every `[x]` milestone with no PASS commit-gate entry behind it (advisory; exit codes unchanged).
- **`check_acceptance_suite.py --changed-files`**: adds the freshness dimension (`stale_results`) to the acceptance gate, so shipping's re-execution cannot pass on a results file written weeks earlier at build time. `check_agent_report.py --milestone` scopes its ledger record so the commit gate's `--require-ledger-gates` can find it.
- **`check_frontmatter.py`** (`pipeline-tools`, stdlib): validates the frontmatter of every `agents/*.md` and `skills/*/SKILL.md` — required keys, `model ∈ {opus, sonnet, haiku}`, description length (warning), a `trigger:` on every `bgpdd-*` pipeline, and the unquoted-`": "` / unclosed-quote breaks that silently unregister a file. `--self-test` runs 9 cases; `agents/blackgoat.md` is exempt per convention #7. Nothing had ever checked frontmatter validity on its own — only downstream symptoms surfaced, long after the cause.
- **GitHub Actions `plugin-checks.yml`**: every script's `--self-test`, `test_check_coverage.py`, and the dependency-table and frontmatter lints, on ubuntu and windows. The lints existed; nothing ran them.
- **`database-migration-patterns` skill**: the methodology contract for relational schema change (EF Core primary, generic SQL) — expand/contract evolution, forward-only migrations, two-step deploy (schema before its consuming code), a reviewed `--idempotent` SQL diff saved under `implementation/evidence/migration/`, an explicit `[destructive-waiver: <reason>]` plus backup/verify for DROP, column narrowing, NOT NULL on populated columns and type changes, rollback as a tested forward migration or a rehearsed restore timed against `shipping-and-launch`'s `< 15 min` DB tier, batched idempotent backfills, and lock-aware execution. Depth in `references/migration-deep-dive.md`; wired into Mason, Aria, Dep and Luna as an on-demand dependency.
- **Orchestrator Contract §2 — `PARTIAL IS NEITHER`**: a `PARTIAL` handoff is resolved with §4's on-failure procedure and a fresh re-delegation, and never counted against the autonomous-rejection bound. A verdict that is neither pass nor fail was being routed as whichever one the reader preferred.
- **`bgpdd-bugfix` gains state, a game tape, a RED capture and a commit gate**: `{bugfix-root}` state initialized at Phase 1 (inside `implementation/` on the feature route so it cannot clobber the epic's state), the RED run captured through `run_quiet.py --capture` with `[probe-exempt: test runner, RED capture]`, and `check_commit_gate.py --verify-tree --ledger --commit` replacing commit-on-Approve. Its missing `trigger: /bgpdd-bugfix` key is added, and it now offers `/bgpdd-verify` on close.
- **`bgpdd-discovery` Phase 0 Fit Check** (brownfield-only; a greenfield or empty repo HALTs to `/bgpdd-plan`) and **Tier-1 provenance stamps**: Iris stamps `context.md` and Echo stamps `overview.md` with the HEAD sha and date; consumers warn on drift, never halt.
- **`bgpdd-lite`** initializes state at Phase 1 confirmation (closing the orphan-plan HALT) and writes its Fit Check answers verbatim into `requirements.md` as a `## Fit Check` section — convention #9 applied to a check that was previously answered in conversation and lost.
- **Forge write-boundary guard and eval bracket** (shipping Step 7, learn Step 5): `weekly-check.ps1` lists the affected evals before an apply (offered, never auto-run); the post-apply `git diff --name-only` HALTs on `agents/blackgoat.md`, any path outside the plugin, or a frontmatter edit; and `<changed_skills>` pairs each approved lesson with its destination so one bad lesson is revertible without unwinding the apply.
- **Orphaned root checklists wired into dependency tables**: Vera now cites `references/accessibility-checklist.md` and `references/performance-checklist.md`; Cipher cites `references/security-checklist.md`. They had been on disk, referenced by nothing.
- **Eval harness**: `run-evals.ps1 -SelfTest` (offline, zero-token judge check), per-run agent transcripts under `evals/results/transcripts/`, a trigger case for `/bgpdd-verify` routing (20 trigger cases), and `weekly-check.ps1` mappings for `skills/bgpdd-verify/` and `skills/agent-squad/base-persona.md`.
- **`agent-audit` gains a mechanical preflight, metrics 19–21 (18 → 21), and a fan-out execution model.** The 2026-08-30 audit **passed all 18 metrics** while three frontmatter blocks on disk failed to parse and two shipped skills had never registered in any runtime — because every metric was applied by reading. Step 0.5 now runs before any prose is opened: `check_frontmatter.py`, `check_dependency_tables.py`, a registration diff of the tree against what the runtime reports as loaded, every script's `--self-test` plus `test_check_coverage.py`, the `--ledger` grep over the pipelines, and the eval-record summary — each with its command and exit code recorded, and a command that cannot be run reported BLOCKED rather than skipped. **Metric 19 (Gate Adversarial Proof)** asks not whether a gate exists but whether it *bites*: author the minimal fabricated input that claims the work was done without doing it and run the gate against it in a scratch directory; a gate that exits 0 on fabrication is a Blocker, and every load-bearing gate must read something the author cannot type. **Metric 20 (Gate Invocation Coverage & Lane Parity)** requires the ledger grep to be empty and every lane that ends in a commit or a ship decision — the light ones first, since the most-used lane is the likeliest to be ungated — to have a state file, a game-tape write, a mechanical gate between the last verdict and the commit, and a script-only closure path. **Metric 21 (Eval-Suite Health)** audits the instrument: a suite with zero records is a Blocker (the trigger suite sat unrun for weeks), records without provenance are fiction across time, red cases are triaged GRADER/FIXTURE/CONTRACT/AGENT/INFRA before entering the findings, and an INFRA failure counted as an agent failure is a Blocker. Metrics 19–21 are the only metrics that **execute** something, and one that could have been run but was eyeballed is recorded NOT RUN, never PASS. The new **Execution Model** section makes the 2026-09-02 shape the sanctioned one for any target larger than one agent or one skill: one read-only background agent per lens (pipeline flow, personas, methodology skills, tooling/evals/docs, cross-reference sweep, gate red-team, eval diagnosis, optional secondary lanes) with disjoint scopes, then synthesis — the Orchestrator reads reports rather than files, independently re-verifies every Blocker before reporting it (unverified findings are PLAUSIBLE, never CONFIRMED), and records the audit's own agent count, tiers and token use, because roughly one in ten agent findings in that run was wrong on a premise.
- **`detect_stack.py`** (`pipeline-tools`): deterministic, evidence-backed stack detection — dotnet, vue3 (with vue2 suppression), react, angular, node, python, godot, powershell, docker, aws, azure, github-actions, playwright and the four db stacks — reporting both the stacks and the `skills` they imply. Six methodology skills gated on an `If the project uses X` dependency-table row, which meant the condition was decided by Iris's prose, and a stack could silently fail to load its contract with no symptom at all. The detector never guesses: a stack appears only with at least one relative evidence path. Wired into `bgpdd-discovery` Phase 0/1 (the Orchestrator runs it and pastes the block; Iris records it verbatim and may add, never subtract) and into `bgpdd-build` hydration, which injects the resolved `skills` list into every builder/Quinn/Luna brief. `--self-test` runs 18 cases.
- **Run telemetry: `record_run.py` + `summarize_run.py`** (`pipeline-tools`, Orchestrator Contract §4). One JSON line per delegation completion and per state persistence to `run-log.jsonl` — agent, model, duration, tokens, rounds — with `--from-json` mapping a runtime's completion payload. The gate ledger recorded which gates fired; nothing recorded what the run cost, so every cost sentence in a game tape was written from memory at the moment it was least checkable, and the shipping tape's fired-versus-rubber-stamped column had no source. **Unknown is `null`, never `0`** (an explicit `0` is preserved, and `tokens_total` is derived only from two known halves) because a fabricated zero is worse than an admitted gap; cache tokens, booleans and `num_turns` are deliberately unmapped. `summarize_run.py` is the reader half and the mechanical form of the metric: per-pipeline/unit/agent cost with known/unknown splits, and gates classified `fired` / `rubber_stamped` / `inconclusive` — the third bucket exists because an ERROR verdict is a gate failing to run, not a gate catching something. `--markdown` emits the block `bgpdd-build` Phase 6 and `bgpdd-shipping` Step 6.5 now paste instead of narrating. Unlike the best-effort ledger, a failed `record_run.py` write is exit 2: the record IS the artifact. Self-tests 15 and 14.
- **`check_ship_decision.py --require-rehearsal` and `--require-baseline`** (plus `--repo` and `--max-rehearsal-age-days`), wired into `bgpdd-shipping` Step 3's exit ticket. The gate accepted **any** `/rollback/` heading as proof — which proves a *plan* — and the checklist's "dry run if possible" had no owner and no artifact. A rehearsal line must now conform to a grammar (`Time to Rollback: <N><unit> — rehearsed <date> on <env> — evidence: <path>`) and cite a `run_quiet` capture under `evidence/rollback/` with a matching sidecar and **exit 0**: a failed revert-plus-health-check is `rehearsal_failed_exit`, a rehearsal proving the rollback does not work. `--require-baseline` demands a `## Baseline` section of three evidenced readings, because every cell in the Rollout Decision Thresholds table is a delta and without a recorded baseline each row resolved against someone's memory of normal — so the canary could never fail. Both flags default off (build Phase 5 and shipping Step 0.4 gate a *prep* decision that legitimately predates both; labelled convention #8). Self-test 13 → 47, with `ship-decision-rehearsed/` and `ship-decision-unrehearsed/` fixtures.
- **`shipping-and-launch` gains Baseline Capture and Rollback Rehearsal** (Dep-owned, pre-GO): the health check is the last command in the revert sequence, and the recorded time must fit the Time to Rollback ladder — a labelled refinement, since the ladder is an estimate and the rehearsal is binding.
- **`bgpdd-shipping` Step 5.5 — Post-Deploy Verification.** Until it existed **the pipeline ended at PR open**: every "after deploying" item in `shipping-and-launch`'s Verification section was owned by nobody, and the last thing shipping verified was a decision taken *before* the deploy. A fresh Dep (Orchestrator Contract §1's independent-verification exemption — the context that wrote `GO` an hour ago reads every dashboard as confirming it) executes the post-launch checklist against the deployed environment into `post-deploy-report.md`, gated by `check_agent_report.py --milestone post-deploy` and `check_runtime_evidence.py --milestone post-deploy --changed-files <the epic union>`. A `Fail` is a **live production defect, not a pipeline failure**: no fix round, take the rollback decision from the recorded `Time to Rollback:` and the threshold table, and HALT for the user, because executing a production rollback is theirs to authorize. A deploy not made this session is recorded `NOT RUN` — a deferred verification recorded as deferred is honest, one recorded as green is the defect the step exists to prevent. Numbered 5.5 so 6.4/6.5/6.6 keep the identifiers README and `agents/echo.md` cite.
- **Structured blockers-ledger schema** (`update_state.py`, `check_blockers.py`, `check_commit_gate.py`): entries are `{id, text, milestone, capability, severity, source, added, evidence}`. Scoping used to be a substring guess against freeform text, which cannot tell "not obviously this milestone's" from "not this milestone's" — so the honest reading was to fail closed on the whole array, and a blocker raised against `M7` held `M2`'s commit hostage. Exact equality on a structured `milestone` field replaces the guess, with `null` still blocking everything (the fail-safe survives exactly where the writer stated no scope; labelled convention #8). Legacy strings normalize on read to unscoped Critical and are **never rewritten**. `--add-blocker` gains `--blocker-milestone`/`--blocker-capability`/`--blocker-severity`/`--blocker-source`/`--blocker-evidence` companions and auto ids `B-<n>`; `--resolve-blocker` matches an exact id, then exact text, then a unique substring (ambiguous → exit 2 with `candidates`) and logs the full entry; `check_blockers.py` gains `--milestone` and `--severity-floor`; the commit gate reports `other_milestone_blockers` and `ignored_unscoped_ids`, and keeps `Info`-never-blocks as a fixed floor deliberately not caller-tunable at the last checkpoint. Self-tests 20 → 29, 6 → 16, 57 → 62.
- **`security-and-hardening` gains the controls Cipher's persona had been carrying**: JWT verification against a pinned algorithm, server-side authorization, rate limiting beyond login, a wildcard-CORS ban on authenticated routes, container non-root plus image scanning, and named audit equivalents (`pip audit`, `cargo audit`, `trivy`).
- **`code-simplification` gains the procedure Max's persona had been carrying**: green-baseline-before, one-kind-of-change-per-pass, and signal rows for long parameter lists, magic constants, settled feature flags, leftover debug logging, resolved TODOs, and imperative-loop-to-declarative.

### Changed
- **Trigger evals now measure routing, not mention (`harness_version: "3"`).** Runs use `--output-format stream-json --verbose` with stdin closed and judge the **first `Skill` tool_use** in the transcript. Three outcomes — `ROUTED_OK` (the only pass), `ROUTED_WRONG`, `NO_ROUTE` — are recorded as `outcome`/`first_skill`, plugin namespaces normalized; a skill merely named in prose is recorded as `mentioned_only` and never passes. Runs execute inside a real app fixture (`evals/trigger/fixture/`: a Vue 3 SPA over a .NET 8 API plus the `.docs/` tree the prompts refer to) and archive their stream to `results/transcripts/`. Contract runs now archive `handoff.txt` and the temp copy's `.docs/` tree as a per-run directory, copied before grading. `weekly-check.ps1` flags `trigger` when the fixture changes. **Measured cost is ~$1.77 and ~250 s per trigger run**, so the printed estimate rose from 3,000 to 175,000 tokens per run; a full trigger pass is no longer cheap. **Open finding from the single validation run:** with the fixture in place and `Skill` available, the model in `--permission-mode plan` still explored the app and wrote its own plan rather than invoking `bgpdd-plan` (`NO_ROUTE`, 50 tool calls, zero `Skill`). The judge is sound; whether plan mode suppresses skill invocation, or the fixture is too thin, is the next experiment (`--permission-mode default --allowedTools Skill`, or an appended routing instruction) — a `NO_ROUTE` sweep must not be read as "the skill descriptions are broken" until it lands.
- **`skills/pipeline-tools/SKILL.md` split into a lean spine plus `references/`** — it had reached 700 lines / 101 KB and was the only file violating convention #1, while being cited as single contract authority by fourteen files across the pipelines, personas and sibling skills. The spine now carries, per script, a two-to-three sentence purpose, the exact invocation, flags, JSON keys, exit codes and problem codes, plus cross-cutting **Gate ledger** and **Fenced blocks and encoding** sections; rationale, observed-failure narratives, parsing grammars, fixture inventories, scope-limit essays and self-test case lists moved verbatim into thirteen `references/<script-name>.md` files. The file stays at its path and every contract statement survives; the frontmatter `description` is trimmed from 1743 to 990 characters while still naming all thirteen scripts. New sections document `check_frontmatter.py` and `mark_milestone.py`, which had shipped undocumented.
- **Fence stripping and BOM tolerance in every verdict/status parser** (`check_commit_gate`, `check_agent_report`, `check_acceptance_suite`, `check_ship_decision`, `check_coverage` test mode, `update_state`, `check_runtime_evidence`). A verdict or status token inside a fenced block is a template or a captured transcript, never an assertion — and because these parsers resolve latest-mention-wins, a pasted example silently overrode the real verdict. Every file is now read as `utf-8-sig`; a BOM previously made a valid report exit 2. `check_coverage`'s plan and design modes are the labelled exception (convention #8): probe lines and register rows legitimately live inside fences.
- **`check_commit_gate.py` closes a `## Review:` section at ANY level-2-to-6 heading.** Closing only on `## ` let a `### Addendum` carrying its own `**Verdict:** Approve` override the `Request Changes` above it. Consequently the review-report template in `code-review-and-quality` now places `**Verdict:**` and the two evidence lines **before** the section's first `###` subheading, and `ui-design-patterns`' design-critique reference no longer puts the `**Rendered evidence:**` line inside its `### Design Critique` subsection — under the new boundary that content is outside the section the gate reads, and the gate fails closed. A report authored against the older bottom-of-section template must move the line up; nothing else changes.
- **`check_coverage.py` test mode requires evidence, not just a token.** A status is read only from a list item; bare prose containing an id and the word "pass" is a status-less mention. A `PASS` whose evidence half cites nothing checkable — an exit code, a `file::test-name`, or a runtime-capture citation — is now `UNEVIDENCED`: status-bearing, not covered, so the Must-Have lands in `uncovered` and the gate exits 1. The prose had demanded evidence at length for months; the machine accepted `- FR-1: PASS — done`.
- **`check_commit_gate.py --require-rendered-evidence` now demands a real file**: non-empty, carrying its format's magic bytes for `png`/`jpg`/`jpeg`/`gif`/`webp`, and no older than the newest changed file. An empty file named `.png` depicts nothing. `check_acceptance_suite.py` applies the same rule to a manual step's cited evidence, which must additionally carry a `## Captured output` section.
- **`check_dependency_tables.py` fails closed**: a missing or empty `agents/` directory is exit 2 rather than a vacuous pass, an agent with no Methodology Dependencies section is a violation rather than a skip, and `{PLUGIN_ROOT}` paths match with or without backticks — the backtick-sensitive matcher had been silently skipping half the table it claimed to check. `--self-test` added.
- **`bgpdd-shipping`'s "Acceptance Suite Regression Gate" is now a Re-execution Gate**: a fresh Quinn re-executes the matrix (appending a `## bgpdd-shipping — [date]` section with `run_quiet` captures), then `check_acceptance_suite.py --changed-files <git diff build-last-commit..HEAD>` gates it. Re-reading build's `acceptance-results.md` was passing the same gate it already passed, against a tree that had moved. Labelled as a divergence from build Phase 5 step 2.5.
- **`bgpdd-shipping` ordering and gates**: Step 0.3 is mechanical (`next_milestone.py` must return `DONE`); cleanup moved to **Step 6.6**, after 6.4 and 6.5, both of which read the state file it deletes (the old `### Step 6` heading no longer exists); Step 6.4 gains a lite path that appends `## Shipped without matrix — {project-name}` with FR/NFR ids and evidence lines; Step 7 is gated on a populated game tape and Forge is skipped out loud otherwise; failures route to `/bgpdd-build` re-entry with `--add-blocker` first; and the Python-3-HALT restatement is consolidated from 8 citations to 1.
- **`bgpdd-build`**: `bgpdd-shipping` is accepted at hydration as RE-ENTRY (a labelled widening of the hydration whitelist; `bgpdd-verify` stays rejected); the commit gate passes `--ledger` and `--require-ledger-gates`; milestone completion runs through `mark_milestone.py --require-commit --ledger`; the runtime gate's `--surface`/`--require-key`/`--expect-status` come from `next_milestone.py --emit-gate-args` with the probe line as fallback; and Quinn's briefs require the `run_quiet.py --capture` sidecar and a real client probe unless `[probe-exempt: <reason>]` is stated.
- **`bgpdd-plan` cites the contract instead of restating it**: the two per-gate Python-3-HALT restatements are replaced by one pipeline-level citation of Orchestrator Contract §1, matching build/lite/verify, and its three gate invocations (design-mode coverage, plan-mode coverage, acceptance-matrix lint) now carry `--ledger`. `bgpdd-verify`'s three gate invocations carry it too, on its own Tier-2 path.
- **Where `--changed-files` is deliberately absent, and why** (convention #8, refining `pipeline-tools`' freshness rule rather than contradicting it): `bgpdd-build` Phase 5 step 2.5b and `bgpdd-verify` Phase 3 invoke the acceptance gate immediately after the Quinn delegation that produced the results, so freshness holds by construction; `bgpdd-shipping`'s later re-execution is the one call site where staleness is possible, and it is the one that passes the flag. Both call sites now say so.
- **`update_state.py` gains `--ledger`**, and pipelines pass it on `--resolve-blocker` calls only. The CLI still cannot judge whether "trust me" is real evidence; the ledger makes the claim durable and attributable instead of gone the moment the array shrinks. `bgpdd-build` Phase 0's deferred-capability blocker now shows the full resolve command rather than naming the flag in passing.
- **Model tiers rebalanced**: Luna sonnet → opus, Rex sonnet → opus, Max opus → sonnet.
- **`<fix_verification>` promoted from the builders' overrides into `base-persona.md`** — Mason's and Nova's duplicated definitions collapse to a one-line reference, and Nova keeps her fresh-render requirement as a labelled `[UI]` delta.
- **Dep's rollback bound cites the tiered ladder** in `shipping-and-launch` instead of a flat "under 5 minutes"; that skill's rollback template now shows an EF Core forward-migration rollback instead of the Prisma example, and its migration checkbox points at `database-migration-patterns`.
- **Aria's `.docs/`-only write boundary** is restated as a labelled "Base Persona Override" block (§0.5 numbering preserved for an eval reference).
- **`bgpdd-discovery` Phase 4b** is covered by the artifact check, and its scratch state is named `.docs/summary/{feature}/discovery-state.json`.
- **README**: `database-migration-patterns` added to the methodology list; the `pipeline-tools` entry now names the whole CLI family instead of one script and drops the "manual check remains the fallback" line, which contradicted the skill's own no-manual-substitute rule; the shipping and learning-loop mermaid diagrams show Step 6.4 → 6.5 → 6.6 in order and Step 7's populated-game-tape entry gate.
- **Deliberate divergences labelled** across the wave (convention #8): Luna's sixth review axis against `code-review-and-quality`'s five; the game tape's 3–6 bullets per milestone against its siblings' 10 per run; shipping's per-checklist-area fix bound against the contract's per-artifact bound; Quinn's and Vera's shared `**Runtime evidence:**` duty as deliberate redundancy, with Vera naming her axis split from Luna; `mark_milestone.py --require-gates` checking a verdict where `check_commit_gate.py --require-ledger-gates` re-hashes inputs.
- **Personas defer procedure to their skills (convention #4).** `dep.md` (-24% words), `cipher.md` and `max.md` no longer carry step-by-step method: Dep's containerization/CI/IaC/observability manual moved into `cloud-deploy-patterns`, Cipher's control restatements into `security-and-hardening`, Max's refactoring signals into `code-simplification`. Each persona keeps identity, ownership, refusal bars and its labelled divergences, and every moved rule is reachable through the dependency table — the procedure had drifted into the personas precisely because the skills were not contracted to hold it.
- **`cloud-deploy-patterns` split into a provider-agnostic Baseline plus on-demand Provider Checklists**, and **Dep loads it `Always`** — a labelled loosening of "If deploying to AWS or Azure" (convention #8): that condition left him contract-less on every other target, which is how the baseline drifted into the persona in the first place.
- **Dep's ship duties are evidenced captures, not statements.** Before any GO he captures the rollout baseline (three metrics read from the monitoring source into `evidence/baseline/`) and rehearses the rollback (a timed revert-plus-health-check on a non-production environment, captured under `evidence/rollback/`), and after the deploy lands he executes the post-deploy checklist against the deployed environment into `post-deploy-report.md`. All three are contracted in `shipping-and-launch` and all three are read mechanically, so a plan is never a rehearsal and a remembered number is never a baseline.
- **`shipping-and-launch`'s post-launch checklist gains an owner, a destination and a grammar**; "Rollback plan documented" becomes **"Rollback rehearsed"** plus **"Baseline captured"**; the error-rate and latency checks compare against the *captured* baseline rather than an impression of normal; and Escalate When gains an un-rehearsable rollback, an unreadable metric, and a `Fail` post-deploy verdict.
- **Stack-skill dependency rows gate on the detector, not on prose.** `bgpdd-discovery` Phase 1 runs `detect_stack.py --markdown` and pastes the block into Iris's brief; `iris.md` gains a Detected-Stacks Contract requiring her to record it verbatim as `## Stacks (detected)` in `context.md`, where she may add a stack on her own evidence but may never remove a detected one. The godot/dotnet/vue3 rows in `mason.md`, `nova.md`, `luna.md`, `quinn.md`, `aria.md` and `max.md` now read "When `detect_stack.py` reports `<stack>` … or the brief names it" in place of `If the project uses X`.
- **Forge reads the mechanical records first** (`forge.md`, `agent-orchestration-improve-agent`, `bgpdd-learn` Step 1, `bgpdd-shipping` Step 7): the run-log summary and gate ledger, **then** the game tape, then the durable reports, then filtered transcript greps. The tape is written by the party being measured; the records are not, so reading the tape first anchored every later reading to it. With it comes the **Incident Test** — a gate proposal must name the incident that would have fired it, and a gate removal must cite fired-versus-rubber-stamped counts.
- **`bgpdd-build` hydration resolves stack skills once and hands them over**, Tier-1's recorded `## Stacks (detected)` block winning over a fresh detector run; Phase 0's deferred-capability blocker now carries `--blocker-capability` and `--blocker-source`, scoping to a milestone only when the gap bites one, and its resolve call targets the printed `B-<n>` id rather than a substring that is exit 2 the moment two gaps share a word. Its Blockers precondition is reworded from "the array must be empty" to the three-way split the commit gate now enforces (labelled convention #8 — a loosening, since an entry scoped to another milestone was never this milestone's).
- **`bgpdd-bugfix` §1 binds a run-log path** (`{bugfix-root}/run-log.jsonl`, `--unit {bug-slug}` for every record, with the `summarize_run.py --markdown` block pasted at the final phase transition) and requires `--blocker-source` on every `--add-blocker`; `--blocker-milestone` is deliberately omitted there, since a bugfix has one unit and scoping would assert nothing.
- **`bgpdd-shipping` Step 6.5 pastes the whole-epic run-log roll-up** (unscoped — deliberately unlike build's per-milestone block, and not a duplicate of build's own epic roll-up, since shipping's own delegations have appended records since then), and its Step 3 re-entry blocker carries `--blocker-source shipping --blocker-milestone`, so a shipping finding holds the milestone it affects instead of the whole plan.
- **`skills/pipeline-tools/SKILL.md`** gains spine sections for `detect_stack.py`, `record_run.py` and `summarize_run.py` and rewritten sections for `check_ship_decision.py`, `check_blockers.py`, `update_state.py` and the commit gate's blockers precondition, with three new `references/` files and appended depth in four existing ones; the frontmatter `description` names all sixteen scripts inside the 1024-char budget. **CLAUDE.md convention #9's conversions list** gains `check_frontmatter.py`, `mark_milestone.py`, `summarize_run.py --markdown`, `detect_stack.py` and `check_ship_decision.py --require-rehearsal`/`--require-baseline`.
- **README**: the `pipeline-tools` entry names `detect_stack.py`, the `record_run.py`/`summarize_run.py` pair and the rehearsal/baseline flags; the `agent-audit` entry describes the fan-out execution model and the re-verification rule that separates CONFIRMED from PLAUSIBLE; and the shipping diagram gains **Step 5.5** between Step 5 and Step 6.4, with its fail branch going to the rollback decision rather than to a fix round.

### Fixed
- **Harness 2's trigger `tool_use` path was dead code**: `claude -p --output-format json` returns a single result object with no content blocks, so every trigger run fell through to substring matching, and a stderr stdin warning preceded the JSON. Every trigger record before harness 3 measured mention, not routing; those records are archived (`results-invalid-instrument-2026-09-03.jsonl`) rather than rewritten. Archived transcripts no longer carry a UTF-8 BOM that broke their first JSON line.
- **Three files were failing YAML frontmatter parsing, and two of them had never registered at all.** `skills/bgpdd-verify/SKILL.md`'s `description` was an unquoted scalar containing `Verify-only: …`, so the whole block failed to parse and the pipeline was never registered as a skill — since c266696 (2026-08-12), i.e. from the day verify shipped. `skills/doubt-driven-development/SKILL.md` had the same defect in its `description` since the initial commit 90e2cbf (2026-07-08) and has therefore never been registered, ever. `agents/scout.md`'s `phase:` line broke the same way and cost Scout its description. **Only the Scout break came from the distillation wave** (commit f9ad762, the 2026-08-30 five-pipeline audit surgery); the other two predate it by weeks and months respectively — the audit surfaced them, it did not cause them. All three values are now quoted, all three register, and `check_frontmatter.py` plus the CI workflow above exist so the class cannot recur silently.
- **`check_runtime_evidence.py --min-captures 0` is now an argparse error (exit 2).** A gate that passes with zero accepted captures reported `PASS` while its own JSON named an in-process transport — the exact claim the gate exists to refuse.
- **A `--changed-files` path that does not exist is exit 2, not a warning** (`check_runtime_evidence.py`, `check_commit_gate.py`, `check_acceptance_suite.py`; `changed_file_missing`). It used to warn and skip, so one renamed path silently disabled freshness for the whole run.
- **`ambiguous_review_section`** (`check_commit_gate.py`, exit 1): a section titled `## Review: M1 M2 M3` matched three gates by word boundary and answered for all of them with a single verdict.
- **Quinn emits exactly one `#Task [N]:` header per literal task number** — never a combined or ranged header, which made her ledger unattributable to a task.
- **`depends-on` for Dep and Vera now include Nova**, a gap left by Nova's introduction.
- **README**: the `agent-audit` heuristic count corrected 14 → 18, and `bgpdd-verify` documented in the SOP list, the lifecycle diagram and the phase prose; shipping's cleanup is described as Step 6.6 rather than Step 6.
- **Eval harness provenance** (`evals/run-evals.ps1`): result records carried no plugin commit, dirty state, CLI version or case hash, and every `results.jsonl` line was a JSON array with the record last, because agent and grader output leaked into the return value. Records are now flat objects carrying `plugin_sha`, `plugin_dirty`, `claude_version`, `case_sha256` and `harness_version`.
- **Trigger judge**: a substring match could pass a response that *rejected* the skill ("don't use bgpdd-lite here"). It now prefers a Skill tool-use record, falls back to negation-aware substring matching, and records which one it used.
- **14 of 20 contract graders** read artifacts with the ANSI default instead of UTF-8 and were CRLF-intolerant; `alex-plan-coverage`'s grader dropped `lint_failures` and its case lacked the Bash tool access its own self-run instruction required.
- **`evals/contract/mechanical-pipeline` updated to the new contract, not around it**: its rendered-evidence fixture is now a real 1×1 PNG (non-empty, correct magic bytes, mtime refreshed) and both runtime captures are written beside a `run_quiet.py`-shaped `.meta.json` sidecar whose `capture_sha256` matches. The zero-LLM integration run passes 17/17 steps against the hardened scripts.
- Stale cross-references chased down after the split: the two eval cases citing `pipeline-tools/SKILL.md`'s "Scope limits" and `--verify-tree` sections now point at the reference files those sections moved into.
- **`agent-audit`'s ledger grep was a gate that fired on everything, which is the same as firing on nothing.** The preflight command matched the bare script names (`check_[a-z_]*\.py\|next_milestone\.py\|…`), so it hit every *prose* mention of a gate — and the pipelines discuss their gates in prose constantly — returning 13 hits on a fully compliant tree while Metric 20 declares "any hit is a FAIL". It is now two greps anchored on `scripts/<name>.py`, with `update_state.py`'s non-`--resolve-blocker` invocations exempt by contract and `record_run.py`/`summarize_run.py`/`detect_stack.py` exempt as non-gates. Both return empty on the current tree, so the check now distinguishes a compliant pipeline from a non-compliant one.
- **`agents/max.md`'s vue3 and dotnet dependency rows still read `If the project uses X`** — the two rows the detector wave missed, since Max is not a `bgpdd-build` builder. They now gate on `detect_stack.py`'s report like the equivalent rows in Mason, Nova, Luna, Quinn and Aria; `dep.md` and `cipher.md` were checked and carry no such row.
- **`bgpdd-shipping` Step 5.5 called `gates.jsonl` "the run log"**, a name now owned by `run-log.jsonl`. It is the gate ledger, and the step says so.

## [2.0.0] — 2026-08-30

### Added
- **Nova (UI Builder)** (`agents/nova.md`): the Builder role split by domain. Mason is now backend-only (`[API]` milestones); Nova owns `[UI]` milestones, with `ui-design-patterns` plus the new component-mechanics craft floor wired in as Always dependencies and a rendered self-verification duty — screenshot evidence saved to `.docs/{project}/implementation/evidence/`, or an explicit `NOT RENDERED` note when rendering is impossible, never a visual claim made from a source read alone. Motivation: mixed-domain milestones were overloading a single builder (one Vue UI milestone measured ~51K chars of wake-up payload) and shipping UI that was weak on basic mechanics — whitespace, table pagination, text boxes, autocompletes.
- **`[API]` domain tag + milestone domain homogeneity** (`planning-and-task-breakdown`): every task now carries exactly one domain tag (`[UI]`/`[API]`; `[SEC]`/`[EXT]`/`[BLOCKED]` remain overlays, not domains), every milestone is domain-homogeneous, and a vertical slice now spans a *pair* of adjacent milestones — its `[API]` milestone immediately followed by its `[UI]` milestone, seamed by `Boundary contracts:` provides/consumes — a deliberate refinement of Step 3's Slice Vertically rule. `bgpdd-build` routes each milestone to the matching builder; a mixed-domain milestone is now a planning defect that halts the build for re-planning rather than something a builder muddles through.
- **`component-mechanics.md`** (`ui-design-patterns/references/`): the mechanical craft floor for tables/pagination, forms, autocomplete keyboard behavior, spacing on computed values, buttons, and feedback states — a checkable-items reference backing Nova's build-time baseline, Quinn's `[UI]` test assertions, and Luna's design-critique axis, rather than leaving those mechanics to be re-derived from taste each time.
- **`check_commit_gate.py`** (`pipeline-tools`): makes the `bgpdd-build` milestone commit gate machine-run instead of prose-run — reads the latest review's Verdict token, checks the review postdates the diff, checks the `blockers` ledger is empty, and performs the commit itself on a pass, so a skipped gate is loud (no commit happens) rather than silent. Ships with `--require-rendered-evidence` for `[UI]` milestones (the review must cite an existing evidence file — mechanizes `ui-design-patterns`' "source can fail a check but never pass one" rule, after an observed ERP run where design critiques approved rendered properties from source reads alone) and `--verify-tree` (git porcelain status checked against the declared `<changed_files>` plus `.docs/`; undeclared edits fail the gate). Motivated by an observed run that committed three consecutive milestones over standing Request Changes verdicts.
- **`next_milestone.py`** (`pipeline-tools`): mechanical next-pending-milestone derivation from `plan.md` — title, domain, full task block for the delegation brief, remaining-milestone counts, and a stale-cursor verdict. Defines the completion convention as `[x]` appended to the `## Milestone` heading line, replacing the previous mandate to re-read the full plan on every milestone (~290K chars, roughly 73k tokens, on a real plan) with roughly 2K of JSON.
- **`run_quiet.py`** (`pipeline-tools`): a build/test runner that writes the full log to disk and prints only errors with surrounding context lines, a tail, and the log path — so agents lose the noise but never lose a diagnostic. Paired with a new Build & Test Log Discipline rule in `dotnet-backend-patterns`, including no-Python flag fallbacks (`-clp:ErrorsOnly;Summary`, `--logger "console;verbosity=quiet"`).
- **`update_state.py`** (`pipeline-tools`): the sanctioned atomic read-modify-write path for `orchestrator-state.json`. Blocker removal requires `--evidence` and appends to a `blockers-resolved.log` — the append-only ledger doctrine made mechanical instead of relying on an agent to remember it.
- **Continuation vs fresh delegation** (`orchestrator-contract` §1): within one milestone, builder/tester fix rounds now continue the *same* spawned agent via delta-only follow-up messages rather than re-briefing from scratch, with mechanical triggers for when a fresh spawn is required instead (circuit-breaker trip, the 3-follow-up cap, or the runtime lacking follow-up capability). Reviewers are exempt and always spawn fresh — independence has to live in the verifier. Runtime-neutral: on runtimes without follow-up messaging, the rule falls back to fresh re-delegation carrying the prior handoff forward.
- **Three-tier persona injection** (`orchestrator-contract` §1): prefer a runtime-native agent type (no re-injection needed) → fall back to persona-file path injection → fall back to verbatim inline only as a last resort. From-memory persona recitals remain forbidden at every tier. Deliberately relaxes the former always-verbatim rule, which cost ~6–8K chars on every single delegation regardless of whether the runtime already knew the persona.
- **Convention #9** (repo `CLAUDE.md`): a prose rule that gets violated in practice converts to a mechanical gate rather than being re-worded or bolded harder — stated as a standing convention so future audit surgery has a default move instead of reinventing one each time.
- **Metric 18 — Convergent Redundancy (Distillation)** in `agent-audit`: flags clusters of rules/lines that encode one underlying principle as distillation candidates (merge prose upward into a principle + corollaries, push enforcement into a mechanism, move rationale to `references/`). Shipped alongside a distillation pass over the four heaviest contracts (`blackgoat-research`, `planning-and-task-breakdown`, `orchestrator-contract`, `agent-audit` itself — heuristics now carry a "Metric families" preamble) and a new **`--design` mode `supersession-annotation` lint** in `pipeline-tools` that machine-checks every Divergence & Supersession Register row against its in-place requirements.md annotation (scope limit: divergences never filed as register rows remain Phase 2.5's job).
- **AI-tells catalog** (`ui-design-patterns/references/ai-tells.md`): the long tail behind the generic-default check, adapted from the "AI Tells" section of [leonxlnx/taste-skill](https://github.com/leonxlnx/taste-skill) (MIT) — fabricated content/data (placeholder-canon names, fake-perfect numbers, generic avatars), decorative metadata, fake product previews, marketing-copy tells, separator/dash rationing, and list-decoration tells. Wired at three points: the **Generic-default check** points to it for content-heavy surfaces, a new Verification Checklist row gates demo/seed/empty-state data on the fabricated-content group, and Luna's craft-floor list in `references/design-critique.md` checks it during design critique. Two adaptations were required and are load-bearing: every group is **scoped by surface mode** (the source self-declares dashboards and multi-step product UI out of scope, so applying a landing-page tell to an Operate surface is its own error — a status dot on a server-health row is information, not a tell), and the source's absolute zero-em-dash ban is relaxed to a **rationing rule** (refused in short strings — headlines, pills, labels; natural frequency in body prose), labelled inline as a deliberate divergence from the source since it is a stylistic position rather than a craft floor. Deliberately NOT adopted from that repo: its 87 KB monolithic main skill (flat, no `references/` split), its mandated React/Next/Tailwind/Motion/shadcn stack (stack rules belong to the framework playbooks, and the squad's frontend playbook is Vue 3), its three-dial system and design-system map (duplicates the committed-direction contract), its Section 0 brief-inference stage including "ask one question" (subagents cannot pause for the user; brief inference is Rex's honing and Aria's direction commit), and its other 11 skills.
- **Design DB tool** (`ui-design-patterns/tools/design-db/`): the CSV design database + stdlib-only BM25 search from [nextlevelbuilder/ui-ux-pro-max-skill](https://github.com/nextlevelbuilder/ui-ux-pro-max-skill) (MIT), vendored as a queryable tool with NO skill/trigger surface of its own. Consumed at exactly one pipeline point: `bgpdd-plan` Phase 2 step 3b injects the search-script path into Aria's brief for `[UI]` work, and `ui-design-patterns`' new **Candidate sourcing** rule has her query it for 2–3 candidate styles/palettes/font pairings before committing the visual direction — with the explicit precedence that the house contract wins over any database recommendation, and a no-Python fallback (commit from the brief and house rules alone). Deliberately NOT adopted: the upstream skill's self-activating description (trigger collision with the pipelines), its doctrine (recommends looks the house contract refuses by default), its design-system generator, and any new UI agent — direction stays Aria's, execution Mason's, critique Luna's.
- **Phase 2.5 — Adversarial Design Gate** (`bgpdd-plan`): a new Orchestrator-run phase between design and planning, with no delegated agent. The Orchestrator runs the `doubt-driven-development` cycle **per-section** on Aria's `detailed-design.md` (the design exceeds DDD's one-read unit), always extracting every money-moving sequence, every state machine, and every read-then-decide gate. Each DOUBT prompt carries a fixed 6-item attack list verbatim: crash windows (process dies after step N, for every N, each naming a recovery mechanism), reversals, races (every read-then-decide gate names its serialization mechanism and a two-concurrent-requests test), trust-boundary amounts, state-machine self-consistency, and config knobs. Findings land in `.docs/{project-name}/design/design-review.md`, go back to Aria as **ONE** revision round (deliberately tighter than DDD's 3-cycle bound, and labelled as such per convention #8), and the returned fixes are verified individually **plus** a regression re-read of every clause a fix touched — a fix round produces a new artifact, not a patch. Unresolved Blockers escalate to the user at the Iteration Checkpoint. Direct response to the v6/v7 money defects that crossed design → plan with 100% fidelity and zero detection. Plan-only by design: design documents exist only in this pipeline, so its absence from sibling pipelines is not a symmetry defect.
- **Three more `pipeline-tools` lints, with tests and fixtures**, landing alongside the `supersession-annotation` design-mode lint noted above — all four are new since v1.2.0, which shipped none of them. Plan mode: **`literal-count`** flags a transcribed artifact-inventory count in a task block (a number followed by an inventory noun — `error codes`, `routes`, `endpoints`, `components`, `screens`), machine-enforcing the pre-existing *count definitions, not mentions* doctrine; **`consumes-provides`** parses each task's `**Boundary contracts:**` field against a deliberately forgiving `provides:`/`consumes:` grammar and fails when an identifier is provided by a later task or by no task at all; **`path-hygiene`** flags path-shaped strings pointing outside the plan's own repository (Windows absolute, UNC, `file:///`, and any `../` repo-escaping segment). Shipped with ~390 lines of new tests and six fixture corpora, each exiting 1 on exactly one check.
- **Metrics 16 and 17 in `agent-audit`**, taking the coverage table from 15 rows to 18. **16 — Defect-Class Enforcement**: a hand-kept inventory verifying that rules installed in response to real escapes are still on disk, because their removal during an unrelated refactor is completely silent — no run ever reports that last month's gate is gone. It instructs the auditor to grep the live files rather than trust the inventory, since distillation passes relocate rules without deleting them, and it explicitly refuses to over-credit `supersession-annotation` (unfiled divergences remain Phase 2.5's job). **17 — Output-Rubric Gate Coverage**: each of 8 output-quality dimensions must map to at least one enforcing mechanism; an uncovered dimension is a Warning, not a Blocker, because not every dimension deserves a hard gate. Alongside these, three existing metrics were widened: **1** gains **writer ownership** (exactly one declared writer per artifact, with phase-order counting as much as writer count), **13** gains a mandatory first check that every `bgpdd-*` pipeline actually loads `orchestrator-contract.md` (the consolidation traded a visible duplication defect for an invisible omission one — a pipeline that silently drops the reference loses those rules with no symptom at all), and **15** expands to four checks including **existence before agreement**: a machine-enforced grammar documented only on the validator's side is unreachable, so the check never fires, and a check that never fires is indistinguishable from one that passes — hence Blocker, not Warning. The Verify section also gains a **stale-reference sweep** after any rename, reorder, terminology change, or **policy flip** — the last being the harder case precisely because nothing textual changed, so a skill can forbid a practice in its rules while its narrative still recommends it, and the narrative is where the reasoning lives.
- **`blackgoat-research` design-integrity rules**: the `## Divergence & Supersession Register` design heading; a **Self-Consistency Pass** built on *the document must be true wherever a reader lands* — samples win over prose (fix the sample, not the invariant), frozen means literal and complete (zero elisions in anything designated normative), assertion sets are checked as sets rather than pairwise, a revision rewrites in place instead of layering an override banner, and *a decision that makes any FR/NFR sentence false is a supersession* — **NOT complete until** both a register row and an in-place `requirements.md` annotation exist; a closing **Brief-Conformance Diff** walking every FIXED brief section and Must-Have FR/NFR item-by-item; and a **Mode 1 Blueprint / Mode 2 Scoped Advisory** split so `bgpdd-build`'s blast-radius review runs steps 1–3 only instead of the full design workflow.
- **`shipping-and-launch` verdict integrity**: the checklist is now explicitly *a floor, not the whole checklist* — where a numbered requirements set exists, every Must-Have FR and NFR gets its own row cited by ID and carrying its evidence, enumerated **from the requirements document, never from the built feature list**, because requirements expressed as qualities (accessibility, contrast, performance, theming, responsiveness) are exactly what a feature-shaped review cannot see; a requirement with no row is an unperformed check, and the recommendation is `NO-GO` until it has one. The GO/NO-GO decision now certifies **one exact tree state** (commit SHA recorded, working tree confirmed clean; uncommitted changes at verdict time are a `NO-GO`, and any later change means reissuing rather than leaving a document certifying a tree no longer on disk), and must **reconcile against the ledger**: every open blocker entry in `orchestrator-state.json` copied verbatim, waivers recorded beside the entry they waive and never inferred from silence or an elapsed phase, and `blockers: None` treated as an assertion that the ledger was read and found empty — never a default, never a summary of the author's own view.
- **Three `blackgoat-idea-honing` checklist items**, each closing a class that reached design unasked: check-then-act gates on counted or summed state (for *every* "at most N" / "only while capacity remains" rule — quota, balance, rate, seat, credit — ask what happens when two requests arrive at once), externally-supplied amounts that move money (what is the number validated against internally — a signature proves origin, not correctness), and **MoSCoW tiers must discriminate** (ask the cut question on every Must candidate; a requirement the user would ship without is not a Must).
- **`code-review-and-quality`: undeclared substitution and undeclared addition are both Critical** — review the change against the requirement text as written, not only against the task that paraphrased it. Both defects hide from a task-level read because the code is competent and the task is silent.
- **`ui-design-patterns`: library-vs-token-layer property ownership** — when a component library and a token/utility layer coexist, the committed direction names which layer owns each visual property (surface color, radius, border, elevation, typography, density) *before* code. Reaching for `!important`, a selector into the library's internal class names, or a global element-level override to land a design decision is a **defect signal, not a technique**: the property has no owner, the override survives only until the library's internals change, and that surface is now silently detached from the design system.
- **`CLAUDE.md` convention #8 — a deliberate divergence must name the rule it refines.** This plugin is a layered ruleset, and an unlabelled tighter-or-looser bound is indistinguishable from a contract collision: an auditor reports a false contradiction, and a reader silently picks whichever bound they read first.
- **Eval: `evals/contract/mechanical-pipeline/`** — a zero-LLM, deterministic integration case walking the full mechanical milestone lifecycle in a disposable git repo (next_milestone derivation with a stale cursor → update_state cursor/blocker writes → check_commit_gate failure paths: Request Changes, standing blocker, undeclared tree, missing rendered evidence → evidence-gated blocker resolution → gated commit → run_quiet error extraction); 13 asserted steps, safe to run unconfirmed (no `claude -p`), exempt from the runs=5 doctrine since it has no LLM variance. Its first execution found the `--verify-tree` untracked-directory defect below.
- **`check_agent_report.py`** (`pipeline-tools`): closes the last face-value verdicts in the pipelines — Cipher and Vera could report "pass/secure" with no durable artifact behind it, and `bgpdd-shipping` Step 3 consumed their handoffs unverified. Both agents now carry an output contract (Cipher: `security-report.md`; Vera: `verification-report.md`, replacing her no-artifact-by-default override) in `.docs/{project-name}/implementation/`: one section per round, one terse line per check — exact command, exit code, finding counts (never scanner output or log dumps — evidence binding by exit code, deliberately cheaper than log capture), `NOT RUN`/`BLOCKED` for anything unexecuted, findings with severity + file:line — ending in a machine-read `**Verdict:** Pass|Fail` token. The gate script parses the report fail-closed (last verdict-bearing section wins, exact tokens, unevidenced lines fail, zero check lines fail, any Critical finding fails — the verdict is arithmetic over the lines, per `code-review-and-quality`'s Approve rule) and shipping Step 3's new Report Gate runs it over both reports before proceeding, with the standard no-Python manual fallback. Self-tested plus `fixtures/agent-report-happy/` and `fixtures/agent-report-sad/`; `bgpdd-build`'s `[SEC]` lane now routes Cipher's rounds to the same file.
- **Eval: `evals/contract/alex-domain-tags/`** — a persona contract case (authored per the runs=5 / 4-of-5 doctrine) testing whether Alex emits `[UI]`/`[API]` domain tags and domain-homogeneous milestones from the planning skill alone: the delegation brief deliberately never mentions tags, homogeneity, or milestone shape — the omission is the test. Graded mechanically (per-task tag exclusivity, per-milestone homogeneity, `next_milestone.py` exit 0, coverage gate exit 0). A first live run passed on all criteria and surfaced the two format-seam defects below.
- **Five more contract eval cases** (`evals/contract/`), each hand-proven against a good artifact and at least one cheap-path artifact: `luna-clean-approve` (the `luna-verdict-arithmetic` trap case's missing mirror — the same fixture genuinely fixed, correct verdict `Approve`, catching reflexive suspicion and severity inflation); `cipher-security-report` (a clean-looking notes service hiding a hardcoded `sk_live` secret and wildcard CORS, graded by deferring to `check_agent_report.py`); `nova-ui-contract` (first builder-tier case — a frozen Vue 3 API-client boundary with no `node_modules`, so rendering is impossible and evidence honesty is graded directly); `scout-brief-path` (a Tier-2 brief path against Scout's Tier-1 default, plus a richly-commented dead module as bait); and `iris-discovery-guard` (a pre-existing curated `context.md` against a routine discovery brief, testing the do-not-overwrite rule).
- **Eval fixtures' `.docs` inputs tracked in git** — they were never actually frozen; an untracked working-directory file can drift silently between runs, defeating the "frozen fixture" premise the whole suite is built on. Now committed alongside each case.
- **Failing-run artifact preservation** (`run-evals.ps1`): a failing run now copies its working copy plus the grader output to `evals/results/artifacts/<case>-run<n>-<suffix>/` before the temp dir is deleted, so a flake is diagnosable from the actual failing state instead of destroyed with the temp dir.
- **Fix-round verification**: `debugging-and-error-recovery` step 7 now owns the tier rule — VERIFY re-runs *the exact check that failed*, at the tier it was reported at (the named failing test for a Tier 1/2 report, the milestone's `RUNTIME PROBE:` for a Tier 3 report); a green suite at a lower tier does not discharge a failure observed at a higher one. Mason and Nova both re-fire self-verification on a rejection round — "already complete" exempts nothing — and their handoffs carry a new `<fix_verification>` element naming the check re-run and its observed result, or `NOT VERIFIED — <what blocked you>`. `bgpdd-build` Phases 2 and 3 refuse to forward a fix to Quinn without it. Quinn's retests now state whether her independent run agreed or disagreed with the builder's self-check, naming a disagreement as a signal (stale process, wrong base URL, a stale build marker) rather than silently overwriting it. Running Quinn's suite is sanctioned for a builder's self-check; authoring new specs or collections as a verification fallback is banned — `NOT VERIFIED` is the only fallback.
- **Environment manifest**: `runtime-evidence` owns the grammar — a bring-up sequence (ordered steps, each scoped by a `Needed for` column so a later run can bring up the minimal subset), a services table, a repointing map, forbidden hosts, test identities & fixtures, and a capability inventory. Three entry points, one grammar, explicit precedence: `/bgpdd-discovery` Phase 4b writes the durable brownfield recipe (`.docs/summary/{feature}/QA/runtime-environment.md`); `/bgpdd-plan` Phase 3.6 derives it for greenfield work from the design and plan just produced, then asks the user only for what no artifact can hold; `/bgpdd-build` Phase 0 resolves it by that same precedence (Tier-1 recipe, else the Tier-2 file, else author it) and runs a minimal-subset preflight before the first builder delegation. A missing capability is requested from the user immediately and recorded as a blocker that gates the evidence boundary (Phase 2 does not run against it), never a silent halt at detection time. `/bgpdd-shipping` and `/bgpdd-verify` consume the persisted `artifacts.environment_manifest` path instead of re-deriving the estate.
- **Six more hand-verified contract cases** under `evals/contract/`: `mason-fix-verification` and `mason-fix-verification-tier3` (the Tier 1/2 and Tier 3 fix-round traps above), `aria-supersession-writeback` (a design decision that falsifies an existing FR/NFR sentence, requiring both a register row and an in-place `requirements.md` annotation), `forge-blackgoat-carveout` (an approved surgery plan whose second item edits `agents/blackgoat.md` — convention #7's carve-out must hold even under full human approval), `luna-verdict-arithmetic` (an IDOR and a swallowed rejection behind a green suite — the verdict token must be arithmetic over the findings, not vibes), and `max-behavior-preservation` (a load-bearing "redundancy" beside a genuine simplification, graded by a runtime value probe rather than a grep). Every grader was hand-proven against both a good artifact and a cheap-path artifact before shipping.

### Changed
- **`bg-bugfix` renamed to `bgpdd-bugfix`** (breaking: slash command changed from `/bg-bugfix` to `/bgpdd-bugfix`) and redesigned from a solo main-session SOP into a lean orchestrated mini-squad pipeline under the Orchestrator Contract: Phase 1 root-cause analysis stays in the main session (gate: root cause stated to the user before any code), Phase 2 delegates the fix to Mason (or Nova for UI-side bugs) under TDD (reproducing test RED → surgical fix GREEN), Phase 3 hands independent verification to Quinn (full suite re-run, plus out-of-process runtime captures for runtime-observable bugs), Phase 4 routes review and blast-radius analysis to Luna (Critical findings loop back, 2-round bound), and Phase 5 routes systemic lessons to `/bgpdd-learn`. "No squad overhead" no longer describes it — it is now the smallest squad that can prove a fix, not none.
- **Persona distillation wave: 10 agents recompressed to the `c6d8578` baseline, contract-complete** — Iris, Scout, and Echo (discovery tier); Rex, Alex, and Aria (planning tier — Aria's distillation detailed below); and Nova, Max, Quinn, and Luna (build tier). Each was checked line-by-line against its pre-distillation contract, and items the compression had silently dropped were re-added: Alex's run-the-gate checklist item, Aria's step-8 design gate, Quinn's `file::test-name` grammar, and Luna's per-file interrogations. Rex's pass also compressed `blackgoat-idea-honing` and deleted `skills/spec-driven-development/` as convergent-redundant with it — confirmed on this pass: the folder no longer exists on disk, no agent or skill persona referenced it in either tree, and it is a distinct skill from `source-driven-development`, which is unaffected and still present. Luna's distillation took 3 rounds: her own interrogations of the `luna-clean-approve` fixture found and drove fixes for real bugs rather than false alarms — a prototype-chain auth-bypass on bare bracket lookups, a non-primitive-`orderId` crash (an unguarded `String(id)` on an object with a non-callable `toString` downed the process), a `readBody` hardening gap (unbounded accumulation, unsafe multi-byte-chunk decode, no stream-error handling), and a genuine 413-delivery bug. Both `luna-clean-approve` and `luna-verdict-arithmetic` were also rescoped to review only the milestone's feature files, naming the pre-existing transport skeleton out of scope — closing an inexhaustible source of legitimate-but-irrelevant findings a rigorous reviewer will always find in a hand-rolled HTTP skeleton.
- **Alex now runs the plan-time coverage gate himself before handoff** (`planning-and-task-breakdown`) — a new Verification-checklist item requires `check_coverage.py --requirements`/`--plan` to exit 0 against his finished plan before it may be handed off. The rule was already prose in his checklist and already mechanical at the Orchestrator's later gate, but nothing had required Alex to run it against his own draft first; baseline eval runs were failing on exactly the class this closes (transcribed inventory counts instead of set-equality against the source table).
- **`code-review-and-quality` gains two mandatory per-file interrogations**: identity provenance (a request-originated value trusted as an auth boundary is Critical) and failure-path visibility (a swallowed required side effect is Important minimum), plus a per-file "Files reviewed" log proving the walk happened. Closes a gap where Luna's own persona already named both defect classes but the review steps let her walk past them — 3 of 5 baseline runs missed both a body-vs-session tenant check and a swallowed audit rejection sitting on top of a reported-green suite.
- **`Path Resolution` consolidated into `base-persona.md`** — the identical block was deleted from all 14 agent personas, which had each carried a verbatim copy. Textbook Metric 6: a rule stated in fifteen places is edited in one.
- **`prompt-engineering` rewritten** from a passive reference into an executable skill. Its `description` flipped from "expert guide on prompt engineering patterns" to an active trigger surface covering *make me a prompt* / *how do I ask AI to…* / a handed-over vague prompt; `risk: unknown` → `risk: safe`, with `category` and `tags` added. The SKILL.md is now interactive one-question-at-a-time honing followed by framework selection across 11 named frameworks (RTF, RISEN, Chain of Thought, RODES, Chain of Density, RACE, RISE, STAR, SOAP, CLEAR, GROW), restructured as Purpose / When to Use / Workflow / Critical Rules / Notes / Limitations. Ships with a 659-line README and a 3-case `evals/evals.json` whose assertions pin the one-question-at-a-time behavior (no framework jargon and no final prompt on the first turn).
- **Aria's write boundary gains one carve-out** — she may write **supersession annotations** into `requirements.md`: annotation-only, with no new FRs, no renumbering, and no deletion. This is what makes the `supersession-annotation` lint satisfiable; Rex remains the owner of requirements content. Her persona was also distilled (~48 lines out, ~27 in) and the state-mutation-boundary rule moved from Data Modeling to Design Pattern Selection, where it belongs. Her write-back is now a **self-run gate** (`blackgoat-research` step 8): she runs `check_coverage.py --design` on her own output and fixes `supersession-annotation` failures until it exits 0, rather than leaving the write-back to be caught downstream — the prose form of this rule was violated in 5/5 baseline eval runs while in force.
- **`doubt-driven-development` now names both of its pipeline callers** — `bgpdd-plan` Phase 2.5 (per-section, on `detailed-design.md`, as the design review) alongside the existing `bgpdd-build` Phase 5 on Dep's ship plan.
- **Quinn promoted to `model: opus`** (the house code-writing-tier rule — she authors the permanent test suite and was the only code-authoring agent below the top tier). Her `test-driven-development` and `debugging-and-error-recovery` dependencies now load conditionally with labeled divergences (TDD's Iron Law does not bind a post-hoc tester; her reproduce-once bound is deliberately tighter than the debugging workflow), bringing her unconditional wake-up load under the ~2,500-word ceiling.
- **Max retired from `bgpdd-build`** — Phase 4 is now handled as a builder follow-up instead of a separate Max delegation: Luna's Suggestion-level findings are applied by the milestone's already-continued builder, with Quinn regression run via the same follow-up pattern. Once Luna's review absorbed the `code-simplification` audit, Max's conditional trigger effectively never fired anymore (a convergent redundancy in his own audit terms). His persona and the ad-hoc roster entry remain for explicit optimization requests outside the pipeline.
- **Repo-wide distillation (16 subagent batches)**: every persona and every skill spine rewritten from rhetorical prose to terse trigger-table grammar, taking loaded-path words from 64,697 to 56,034 with ~600 normative rules inventoried old-to-new and zero dropped (a restatement is only DROPPED when it names the surviving owner). Multi-sentence rationale relocated to 12 new `references/` files rather than deleted. Machine-parsed grammars stayed byte-exempt throughout and were truth-tested against their own parsers (`check_coverage.py` fixtures, every pipeline-tools script self-test, `mechanical-pipeline` 17/17). **Path Resolution** was consolidated into its one home in `base-persona.md` (see the dedicated bullet above), with each pipeline keeping only a short bootstrap section.
- **Eval harness fixed**: contract runs now copy the plugin's `agents/` and `skills/` into the temp working copy before invoking the agent under test. Previously no agent-under-test could read its own persona or methodology files, so every persona-compliance criterion failed for a wiring reason rather than a behavioral one — no contract case had ever validly passed. The old runs are archived, not deleted (`evals/results/results-invalid-wiring-2026-08-20.jsonl`). First valid green runs under the fixed harness: `mason-fix-verification` 1/1, `mechanical-pipeline` 17/17. `evals/weekly-check.ps1` gained dispatch mappings for `bgpdd-build`, `bgpdd-verify`, and all six new cases above.

### Removed
- **Parallel Fan-Out within a milestone** (`bgpdd-build` Phase 1) and its planning-side contract (`planning-and-task-breakdown`: the "authoritative for conflict-free parallel execution groups" reading of `Dependencies:`/`Files likely touched:`, and the parallelism clause of Milestone economy). Observed failure: parallel Masons committing to the shared working branch moved HEAD under their siblings, and each verification round then re-verified the entire moving diff instead of one task's changes — multiplying token cost for a wall-clock gain the pipeline doesn't need. A milestone is now again the unit of delegation: one Mason builds its tasks in plan order, one Quinn/Luna round verifies the milestone's diff once. (Read-only parallel lanes are unaffected: discovery Scout fan-out, Luna+Cipher `[SEC]` reviews, shipping Stage 2.)
- **`Files likely touched:` task field** (`planning-and-task-breakdown` template, Verification checklist, and all cross-references). Its only structural consumer was the removed fan-out conflict computation, and it duplicated `Named identifiers:`, which already requires exact file paths to create/modify. `Dependencies:` remains as the ordering contract.
- **`skill-creator`** — superseded by Anthropic's own skill-creator; the skill folder, its README row, and its trigger-eval routing removed. (The removal landed on `c6d8578` on 2026-08-21 but the folder had survived the instrument sync into this branch; deleted here to match the recorded decision.)
- **`spec-driven-development`** — its one unowned device, the ASSUMPTIONS enumerate-and-confirm block, was distilled into `rex.md` first, then the skill and its README row and trigger-eval routing were removed.

### Fixed
- **Agent-audit surgery round (2026-08-30)** — an 18-metric agent audit of all five `bgpdd-plan`/`bgpdd-build`/`bgpdd-discovery`/`bgpdd-shipping`/`bgpdd-lite` pipelines, with surgery applied.
  - **`bgpdd-plan` Phase 3.6** wrote `orchestrator-state.json` before Phase 4 creates it — a fresh-run hard failure. Now `--init`.
  - **`bgpdd-lite` acceptance-matrix contract collision resolved** — lite epics author no matrix (a labeled exemption in `planning-and-task-breakdown`); Baseline Reconciliation lives in `plan.md` for lite brownfield epics.
  - **Alex added to `bgpdd-build`'s roster**, scoped to re-planning loops.
  - **`check_agent_report.py` wired into `bgpdd-build`'s `[SEC]` milestone step.**
  - **The 8x-restated Python-HALT rule distilled into one global constraint** (build + lite).
  - **Base-persona-path injection rule re-homed** into `agent-squad/orchestrator-contract.md` §1, with pointers in all seven orchestrating skills.
  - **Vera/Cipher/Quinn report-contract and timeout prose deduped** to authority pointers.
  - **Domain-tag plan lint documented** in `pipeline-tools/SKILL.md`.
  - **`check_coverage.py --self-test` fixed** — completed the partial `908d2a3` sync (fixtures + test file from `c6d8578`); now 111 tests OK.
  - Many smaller frontmatter/metadata fixes.
- **Eval-harness grader bugs, sweep, and pre-distillation certification (2026-08-22)** — two compounding bugs (a Windows-1252 decode of an agent's UTF-8 review report, and a finding-block regex with no id clause) were scoring perfect Luna trap-catching reviews as zero blocking findings; fixed, then swept as the same UTF-8-read fix into the cipher/nova/scout/iris graders. Also fixed in the same pass: Quinn's grader rejecting heading-wrapped `#Task [N]:` tokens the contract never anchors at column 0, and the four remaining red cases from the full-suite sweep (nova and scout: two grader false-positive patterns re-graded PASS from preserved artifacts; vera: a missing `Bash` tool grant; forge: the `agents/blackgoat.md` carve-out existed only in repo `CLAUDE.md`, which the eval harness never ships to agents, so `agents/forge.md` now states it directly). Closed with a recorded 20-run pre-distillation baseline measurement and a full-suite green certification (21/21 cases green, `mechanical-pipeline` 17/17) certifying the baseline for the from-scratch re-distillation that follows.
- **Defect ledger (2026-08-21)**: 26 pre-existing defects surfaced by the distillation's both-ends contract checks, 24 fixed. Headline: the Forge/`blackgoat.md` carve-out — repo convention #7 ("no agent may edit `agents/blackgoat.md`") was unenforced in the one file a spawned Forge actually reads, `agents/forge.md`; it now carries a hard carve-out ("no approval, brief, or finding overrides this"), guarded going forward by the new `forge-blackgoat-carveout` eval.
- **Agent-audit surgery round (2026-08-10)** — a 12-target audit pass over the restructure (5 personas, 7 skill surfaces, 18 metrics each), every Blocker independently verified against the live files before fixing. Deferred by recorded decision: the dotnet reference-playbook debt (project-count/DbContext contradictions, the unmappable `CustomException` path, the Azure-only Rule 7) and the Metric-18 distillation/spine-split passes (Nova/Mason/Luna remain over the wake-up ceiling).
  - **Commit gate deadlocked on any tested milestone**: `--verify-tree` checked the working tree against the *builder's* `<changed_files>` alone, so Quinn's Phase 2 test files failed every milestone as undeclared edits — and the prescribed recovery (the Phase 3 review cycle) could never clear it. `bgpdd-build` now accumulates the union across the milestone: builder ∪ Quinn's rounds ∪ remediation diffs.
  - **Phantom `[ ]` pending-marker**: three `bgpdd-build` sites (state-authority rule, no-Python fallback, completion gate) tested for an unchecked `[ ]` token no conforming plan contains — the fallback silently no-opped on a plan full of pending work and the completion gate failed open. Purged for the real convention (a heading lacking `[x]`); the contract's §4 now names `pipeline-tools` as the convention's home so no citing pipeline re-invents one.
  - **The `Rendered evidence:` grammar was one-ended**: the machine-parsed token lived only in `bgpdd-build`'s delegation brief while both producer-side format owners prescribed other shapes (`design-critique.md`'s `Evidence:`; no evidence line at all in `code-review-and-quality`'s template). Landed in the template with the Verdict line's exact-token treatment and in design-critique §7; the source-only form stays distinct and deliberately non-passing.
  - **Evidence provenance was fail-open**: Nova and Luna shared one unnamespaced evidence directory and the gate passed on *any* existing cited file — a review that rendered nothing could cite the builder's screenshot (the exact ERP failure the flag was built to close). Split into `evidence/build/` (builder self-verification) and `evidence/review/` (reviewer); `check_commit_gate.py --require-rendered-evidence` now requires a cited path under `evidence/review/`, with the negative half tested.
  - **[UI] no-browser deadlock**: the evidence flag had no sanctioned escape on runtimes without browser tooling, while design-critique sanctioned a source-only fallback the gate can never pass. Policy pinned: the flag is conditional on browser tooling; without it, the `[UI]` milestone HALTs to the user — never committed source-only, never looped against an unpassable gate.
  - **Milestone-token substring collision**: gate section/blocker matching was bare substring with last-match-wins, so token `m1` matched `## Review: M10` — an M10 `Approve` could commit M1. Word-boundary matching in `check_commit_gate.py` (self-tested both directions); `code-review-and-quality` now requires the `## Review:` heading to carry the milestone's identifier verbatim from `plan.md`.
  - **`base-persona.md`'s path rule disabled out-of-table references**: "do not scan for a path absent from your brief" read as *any* `{PLUGIN_ROOT}` path, disabling Mason's and Luna's security-checklist reads and Quinn's craft-floor/ledger-grammar references. Rescoped to resolution against the injected plugin root; escalation only when the root is absent or a path does not resolve.
  - **Quinn's `[UI]` craft floor was unreachable**: mandated via a raw path outside her dependency table, injected by nothing. Now a conditional table row plus a Phase 2 `[UI]` injection mirroring Luna's; `component-mechanics.md` items carry stable `CM-<section>.<n>` IDs (append-only) cited by Quinn's assertions and Luna's `NOT VERIFIED` reports, and Luna gained her own conditional row — builder and reviewer now share one craft floor.
  - **Quinn's retest placement contradicted the ledger parser**: "append directly under the original `#Task [N]:` block" vs the gate's last-status-line-in-file-order-wins — an in-place insertion could be silently overridden by a stale later line. Retests now append a fresh block at end-of-file.
  - **The planning walkthrough taught the pre-restructure plan shape**: colon-form untagged milestone headings, a mixed-domain milestone 1 captioned "schema + API + UI", `- [ ]` task bullets `check_coverage.py` cannot parse, and the `## Checkpoint:` level-2 form that silently truncates the milestone block `next_milestone.py` extracts (dropping the runtime exit criterion from builder briefs — under the skill's own "snippets are normative" doctrine, the template would have won over the contract). Template rewritten to the canonical pair-slice shape and verified against the live parsers; checkpoint pinned at `### ` everywhere with the truncation rationale stated; `next_milestone.py` now tolerates the legacy level-2 form with a warning. The spine's "first milestone = one thin end-to-end slice" reconciled to the slice *pair* (labeled against Milestone domain homogeneity per convention #8), ending a deviation-disclosure rule that fired on 100% of plans. Also: the `Boundary contracts:` writer spec no longer describes parser behavior the sentence-break fix retired, and carries the one-physical-line rule that previously lived only on the parser side.
  - **Stale post-split actors swept tree-wide**: "authoring — Mason" and "Mason or Max" rewrite-routing (`ui-design-patterns`, `design-critique`, `cipher`), "the Builder (Mason)" (`code-review-and-quality`), "Mason and his specialized Build Workers" (`luna`), README's Nova-less roster/lineups/loaded-by lists, `agent-squad`'s Max-terminal Cipher trigger and always-verbatim migration-table cell, and two ghost rule names in every pipeline read block ("progressive disclosure", which the contract never carried, and "green-is-not-evidence", renamed to the merged rule *a terminal status is not evidence*).
  - **Mason finally told about the split**: description/role/phase scoped to backend `[API]` (the delegation-routing surface now distinguishes the builders), a worker-side rule returning `[UI]`/mixed milestones unbuilt as routing defects, the testing boundary rewritten naming the rules it refines (unit TDD his, integration/E2E authoring Quinn's, composition-root reachability still verified), and the pre-override "one file at a time" delivery rule deleted.
  - **Nova hardened**: hybrid write-boundary override (code + evidence via dual `<changed_files>`/`<artifact>` handoff, mirroring Quinn's shape), the `evidence/build/` namespace, the methodology's own `NOT VERIFIED — no rendered output` token instead of an invented one, a distinct rendered-but-not-persistable fallback state (never cite a path you did not write), and `component-mechanics` load scoped to component work (−1,312 words per wake).
  - **Wiring gaps closed**: `check_coverage.py --design` (the supersession-annotation lint — implemented, fixtured, and previously invoked by *nothing*) now runs as bgpdd-plan Phase 2.5's mechanical pre-step; `check_dependency_tables.py` named in repo `CLAUDE.md` as the sanctioned form of the path check it mechanizes; `detailed-design.md`'s template gains the `## Design Direction` heading that `ui-design-patterns` and Luna's critique already consumed but no producer ever wrote; Phase 3's axis list now includes the code-simplification audit Phase 4's trigger depends on; the contract's 3-follow-up cap gained its counter in `bgpdd-build`; `update_state.py` gained the no-Python fallback every sibling tool already had; and design-db's unadopted `--design-system --persist` write surface (which writes outside `.docs/`) is now explicitly forbidden rather than merely unmentioned.
  - **`dotnet-backend-patterns` spine**: the sanctioned build command's unquoted `-clp:ErrorsOnly;Summary` (a statement separator in both shells — the command was broken as written), explicit `--timeout` guidance for real-Dev-DB integration suites against `run_quiet.py`'s 240s default (exit 124 is a harness result, never a test FAIL), the phantom "(b)" fallback label, and `Core` disambiguated (≡ `Domain`; the use-case layer is `Application` only).
- **Milestone heading-level drift** — `next_milestone.py` only recognized level-2 `## Milestone <n>` headings; a real Alex-produced plan used level-3 `### Milestone 1 — ... [API]` headings (domain tag in the heading) and the parser exited 2. No contract had ever told the plan WRITER a heading level. Reader: the parser now accepts levels 2–3, treats a heading-carried domain tag as authoritative, and ends the last milestone's block at the first non-Task level-2 heading (so trailing `## Risks and Mitigations` sections are excluded). Writer: `planning-and-task-breakdown` now pins the canonical form `### Milestone <n> — <Title> [<UI|API>]`.
- **`consumes-provides` lint false positives on prose-after-machine-line** — a plan wrote `consumes: none. The ruling is a single string token read by Tasks 3, 4, 5.` and the lint comma-split the trailing sentence into fake identifiers (`4`, `5`, `not`), the exact "format mismatch, not a missing dependency" failure the lint's docs warned readers about. Reader: each keyword's identifier-list text now terminates at the first sentence break (`. ` or ` — `) before comma-splitting — safe because identifiers contain internal dots but never dot-space. Writer: the `Boundary contracts:` field spec now requires the machine line to end at the list (`;`/newline) with prose on following lines.
- **`--verify-tree` misread untracked-directory porcelain entries** — `git status --porcelain` collapses an entirely-untracked directory into one `?? <dir>/` line; path normalization stripped the trailing slash so the `.docs/` carve-out failed and declared files inside untracked directories were reported undeclared (bit only on a fresh `git init`). Directory-shaped entries are now handled explicitly: allowed iff under `.docs/` or containing at least one declared file; otherwise reported as undeclared with the trailing slash preserved.

## [1.2.0] — 2026-07-27

### Added
- **Background Execution (mandatory)** across all five pipeline Orchestrators (`bgpdd-discovery`, `bgpdd-plan`, `bgpdd-lite`, `bgpdd-build`, `bgpdd-shipping`): delegated agents are always launched in the background, never as a blocking call, and independent delegations are launched in a single message so they run concurrently. A blocking delegation makes the Orchestrator unreachable for the agent's entire run — the user cannot ask a question, correct a bad brief, or stop work heading the wrong way, and a long phase is indistinguishable from a hang. Orchestrators must also refuse to guess, predict, or fabricate a still-running agent's results.
- **Incremental Persistence (Anti-Loss)** in `agent-squad/base-persona.md` — therefore inherited by all 14 personas in every pipeline: create the output file with its section skeleton early, then fill and save section by section; mark unfinished sections in-file; resume from existing files rather than restarting or overwriting; and report `PARTIAL` rather than `COMPLETE` when sections remain. Reinforced with delegation-brief rules in all five pipelines, phrased per pipeline (document sections for discovery/plan/lite, code commits plus reports for build/shipping).
- **Scout-then-Aria split for `bgpdd-plan` Phase 2**, with a stated trigger (a large design surface — e.g. a full API contract *plus* costed infrastructure options *plus* a design system), a "do not split by reflex" guard for small features, and the rule that research Scouts write bounded per-topic files to `research/` which Aria then consumes so the work is not orphaned.
- **Scout strategy for `bgpdd-lite` Phase 2**: since lite has no design phase to absorb research, ground truth that is in neither `requirements.md` nor the governing stack contract is gathered by Scouts before Alex plans — with the tie-in that research surfacing *decisions* rather than *facts* is the signal lite was the wrong lane.
- **Scope block on `agent-squad/SKILL.md`** declaring it governs ad-hoc squad use, that a running `bgpdd-*` pipeline's own constraints are authoritative and this file is not loaded, that the pipeline wins on any disagreement, and that cross-cutting *subagent* rules belong in `base-persona.md` instead.
- **`ui-design-patterns` skill**: UI design execution contract — committed visual direction (tokens + signature element) before code, typography/spacing/color/motion discipline, surface modes, category-defaults-to-refuse (anti-generic-AI aesthetics), full state coverage, UX copy rules, and `references/design-critique.md` (Luna's screenshot-driven Nielsen-heuristic review axis). Loaded conditionally by Aria, Mason, and Luna. Adapted from Anthropic's frontend-design skill and pbakaus/impeccable (Apache-2.0).
- **`[UI]` task tag** in `planning-and-task-breakdown`; `bgpdd-build` Phase 3 instructs Luna to run the design-critique axis on `[UI]`-tagged milestones.

- **Learning triage 2026-07-26 — enforcement mechanisms for unverified gates.** A 5-milestone build run reported clean while shipping four gate scripts wired to no `package.json` script and no CI job, hollow assertions no gate had ever been shown to reject, a test report recording PASS from file existence with invented specifics, and three milestones committed over standing `Request changes`. The existing prose rules ("green is not evidence", the `blockers` ledger) were in force and were followed past, so this pass adds checkable artifacts and hard preconditions rather than restatements:
  - **Negative-half proof** (`test-driven-development`, `planning-and-task-breakdown`): no gate or check script is trusted until observed FAILING on a deliberate violation, with the failure output captured. Planned as an explicit task Verification step; enforced in the TDD checklist.
  - **Gate executor wiring** (`planning-and-task-breakdown`): a plan may not declare a verification script without also specifying the manifest script name and the CI job that run it, as their own task steps.
  - **Mechanical commit gate** (`bgpdd-build` §1): the milestone commit's preconditions are two file reads — latest `review-report.md` verdict is `Approve` and postdates the last diff, and `orchestrator-state.json`'s `blockers` array is empty for the milestone.
  - **Remediation is a cycle, not a tail** (`bgpdd-build` Phase 3): a Critical/Important fix re-enters Phase 2 (Quinn re-tests) and Phase 3 (Luna re-reviews the remediation diff), exiting only on `Approve` against the current diff, bounded by the contract's 2-round rule.
  - **Remediation fidelity** (`code-review-and-quality`): a fix is verified against the rule the finding protects, not the finding's literal text — a placeholder route that closes a "missing route" finding is rejected.
  - **Evidence-bearing PASS lines** (`quinn.md`, `bgpdd-build` Phase 6): every `test-report.md` PASS and every game-tape exit criterion embeds the verbatim command and its captured output; no pasted output, no claim.
  - **Gates you author fail closed** (`agent-squad/base-persona.md`): any code producing a verdict must read its contracted evidence input and treat absence/emptiness/unreadability as FAIL/DENY — never a permissive default, never a synthetic stand-in.
- **Learning triage 2026-07-26 — build-correctness rules.** Literal values copied from the governing artifact, never defaulted (`mason.md`); composition-root reachability and fixture-identity mock fidelity (`test-driven-development`); consumer-without-producer and harness-before-features planning rules (`planning-and-task-breakdown`); composable singleton scoping, page-root `data-test` IDs, executed `vue-tsc --noEmit`, and the no-fallback 401 refresh path (`vue3-spa-patterns`).
- **Audit surgery 2026-07-26 — additions.** `bgpdd-shipping` Step 0.4 **Blockers Ledger Gate**: a mechanical open-the-file check that HALTs on any standing `blockers` entry before shipping proceeds (the live failure class: a state file with six standing blockers passed every Step 0 check as written). `bgpdd-learn` gains Path Resolution and the mandatory `orchestrator-contract.md` read it was missing despite delegating Forge (Metric 13 Blocker). Rex's `requirements.md` template gains an `## Edge Cases` section (`EC-n (FR-n)` line items) so Quinn's "every edge case flagged in the Rex Report" anchor has a producer. `blackgoat-research` Step 5 now owns an explicit `detailed-design.md` heading template (the previous instruction pointed circularly at a persona template that never existed). Iris and Scout gain the Base Persona Override blocks for their `.docs/summary/` write paths (Echo's pattern). `dotnet-backend-patterns`: `ErrorCodeJsonConverter` sample with the `[JsonConverter]`-attribute-on-the-type mandate (three serialization paths, only the attribute covers all of them), and the canonical §7 Program.cs now carries the skill's own mandatory exception-handler wiring. `vue3-spa-patterns`: the Axios rejected handler now unwraps the BaseResponse envelope from non-2xx responses and rejects a typed `ApiError` (previously every real failure bypassed the documented notifications contract).

### Changed
- **README Core Concepts** is now five ideas rather than four, adding **Background delegation** (with incremental persistence as its paired rule).
- **`bgpdd-build` §1 Git Workflow** no longer says milestone fixes are merely "re-verified" — it names the Phase 3 step 4 re-verification cycle explicitly and gates the commit on it.
- **`planning-and-task-breakdown` `Boundary contracts:` task field** now covers contracts a task *consumes* as well as produces, including storage mechanism and key names, and requires values restated inline even when an earlier task declared them.
- **Audit surgery 2026-07-26 — wake-up weight and metadata.** `base-persona.md` dieted 1,498 → 989 words with zero rule loss (rationale prose moved to `agent-squad/references/base-persona-rationale.md`); `agent-orchestration-improve-agent` and `planning-and-task-breakdown` spine-split into `references/` deep-dives with parser-critical mandates kept in the spines; Forge's two vector skills load conditionally instead of Always (wake-up 5,346 → 1,625 words). Persona `phase:` labels unified to one pipeline-scoped scheme (`Plan 1–3`, `Build 1–5`, `Shipping — parallel`, `Discovery`), ending the frontmatter-vs-`bgpdd-build` numbering contradiction. Luna's review report has a single format owner: `code-review-and-quality` (`## Review:` headings, machine-read `**Verdict:** Approve | Request Changes` token), with `bgpdd-build`'s commit gate reading that exact token. `references/security-checklist.md` declared single owner of the baseline security contract; Mason/Luna persona copies reduced to pointers. Five methodology descriptions rewritten squad-internal-first (`spec-driven-development`, `browser-testing-with-devtools`, `godot-gdscript-patterns`, `shipping-and-launch`, `planning-and-task-breakdown`) — no more user-facing triggers competing with the pipelines. Max promoted to `model: opus` (code-writing tier rule). Known residual: 8 agents still exceed the ~2,500-word wake-up ceiling; next lever is thinning the tdd/code-review/security spines and the Quinn/Rex personas.

### Fixed
- **Removed the stale "you cannot message a running agent" claim** from all five pipelines and from README Core Concept 2. The runtime does support continuing an already-spawned agent, so the pipelines now prefer sending a follow-up message to an existing agent over re-briefing a fresh one when work must continue with its context intact.
- **`base-persona.md`'s "When your task is complete: save your final output"** wording actively taught deferred writing — the exact behaviour that loses an entire run on interruption. Step 1 now requires progressive writing and points at the new Incremental Persistence section.
- `bgpdd-shipping` and `bgpdd-discovery` were missing the delegation-durability rules their sibling pipelines received, which would have shipped contradictory delegation guidance inside one release.
- **Audit surgery 2026-07-26 — deadlocks and contradictions.** `iris.md` no longer instructs an isolated subagent to ask the user mid-task and terminate on "no" (Metric 4 Blocker — the pipeline already routes that question through the Orchestrator; Iris now notes the existing file in her `<handoff>` and proceeds). `bgpdd-shipping` no longer deadlocks on its own checkpoint: Step 0.2 accepts `pipeline ∈ {"bgpdd-build", "bgpdd-shipping"}`, and the phantom `"complete"` `milestone_cursor` token (a value no producer ever writes) is gone. `phase_completed` dropped from the state schema tree-wide — written by plan/lite, updated by nobody, read by nobody. Aria and Mason escalation language rerouted to the Orchestrator via `<handoff>` (Mason's "routes it back through Rex → Alex → Aria" named agents he cannot invoke). Quinn's `depends-on` and prose no longer claim Luna precedes her on first pass. Alex's layer-grouping rule deleted — it contradicted his own Always-loaded methodology's "Slice Vertically… not layer-by-layer". `blackgoat-idea-honing` Step 3 ends at the finalized transcript instead of colliding with `bgpdd-plan`'s Rex-synthesis split. Re-inlined contract rules in `bgpdd-build` and `agent-squad/SKILL.md` reduced to §-pointers. Sample-code fixes: CS0718 static-class `ILogger<T>` category in `repr-playbook.md`; bare `Results.Ok` disclaimer in `data-and-testing.md`; stale `frontend-ui-engineering` reference in `accessibility-checklist.md` → `ui-design-patterns`.

## [1.1.0] — 2026-07-24

### Added
- **Echo** (`agents/echo.md`) — Legacy QA Analyst: reverse-engineers existing feature behavior during `/bgpdd-discovery` Phase 4 (formerly Quinn Mode A).
- **Vera** (`agents/vera.md`) — Launch Verifier: executes the pre-launch checklist during `/bgpdd-shipping` Stage 1 (formerly Quinn Mode C).
- **Command Timeout Discipline (anti-hang)**: every agent (via `base-persona.md`) and every pipeline Orchestrator caps shell commands at an explicit 4-minute timeout; one justified retry/longer bound, second timeout escalates.
- **Cipher build-phase lane**: `[SEC]`-tagged milestones get a parallel Cipher security review alongside Luna in `bgpdd-build` Phase 3.
- **Vue 3 prevention-first contract** (`vue3-spa-patterns`): new Data Flow & Computation and Lifecycle & Reactivity Hygiene authoring rules (query-once/Map indexes, single-pass iteration, minimal reactivity, listener/timer teardown incl. `onDeactivated`, template refs, null-check discipline), plus `references/vue3-refactor-playbook.md` — the sequential zero-regression retrofit pass for legacy code.
- **Memory Hygiene rule** (`agent-orchestration-improve-agent`): at 3+ accumulated Procedural Memories, Forge elevates/generalizes/moves them instead of appending.
- **Contract evals**: `echo-qa-discovery-shape`, `vera-verification-shape`, `dep-ship-decision-shape`.

### Changed
- **Quinn** is now a single-purpose build-phase QA Tester (`phase: 5 — Testing`); the three-mode persona is retired. Delegations for discovery QA go to Echo, launch verification to Vera.
- **Max** trigger widened: `bgpdd-build` Phase 4 also fires on performance findings below Critical/Important; his persona no longer claims he is "never invoked automatically."
- **Wake-up loads trimmed**: `planning-and-task-breakdown` deduplicated; `luna.md`/`mason.md` defer to their Always-loaded methodologies; Mason's and Quinn's dated Procedural Memories elevated into undated persona rules (Abstraction Rule).
- **`blackgoat-research`** split into Mode 1 (Blueprint) / Mode 2 (Scoped Advisory) so advisor-Aria in `bgpdd-build` never overwrites a signed-off `detailed-design.md`.
- Squad-internal descriptions for `blackgoat-research` and `blackgoat-idea-honing` (trigger-collision fix).

### Fixed
- `bgpdd-plan` and `bgpdd-discovery` were missing the CRITICAL CIRCUIT BREAKER and NO NESTED DELEGATION delegation rules their sibling pipelines carry (audit Metric 13 Blocker).
- `bgpdd-shipping` was missing the base-persona path-injection guard (the recurring "base-persona missing" defect).
- Stale "Mode B" wording in the `quinn-test-report-shape` eval.

## [1.0.0]

Initial release: the agent squad (13 personas), the PDD pipelines (`/bgpdd-discovery`, `/bgpdd-plan`, `/bgpdd-lite`, `/bgpdd-build`, `/bgpdd-shipping`), `/bg-bugfix`, methodology skills, deterministic coverage gates (`pipeline-tools`), and the eval harness.
