{"prompt": "We need to add a whole new checkout flow to the app, nothing like it exists yet - can you help me spec it out and plan it?", "expected_skill": "bgpdd-plan", "acceptable_alternatives": ["bgpdd-lite"]}
{"prompt": "I already know exactly what I want: add a /health endpoint that returns 200 and today's build SHA, same pattern as our existing /metrics endpoint. Just write mini-requirements and plan it.", "expected_skill": "bgpdd-lite", "acceptable_alternatives": ["bgpdd-plan"]}
{"prompt": "The export button on the reports page throws a 500 whenever the date range is left empty - can you find out why and fix it?", "expected_skill": "bgpdd-bugfix", "acceptable_alternatives": []}
{"prompt": "Users are saying search is unusably slow on the dashboard. I don't think this is a quick patch, it's probably the whole query layer that needs rethinking.", "expected_skill": "bgpdd-plan", "acceptable_alternatives": ["bgpdd-bugfix"]}
{"prompt": "Take the plan.md sitting in .docs/webhook-notify/ and go build it.", "expected_skill": "bgpdd-build", "acceptable_alternatives": []}
{"prompt": "Can you go through Rex and Alex's persona files and check for contradictions or dead ends before I hand this squad off to a new team?", "expected_skill": "agent-audit", "acceptable_alternatives": []}
{"prompt": "That was a rough session - Claude kept forgetting to update the plan.md verification steps every time we revised a task. Let's make sure that doesn't happen again.", "expected_skill": "bgpdd-learn", "acceptable_alternatives": []}
{"prompt": "I have a rough idea for a customer loyalty points system but honestly haven't thought through most of the details yet.", "expected_skill": "bgpdd-plan", "acceptable_alternatives": []}
{"prompt": "We already agreed on the schema and API shape for the notifications service in yesterday's meeting - just write the mini-reqs and get Alex planning.", "expected_skill": "bgpdd-lite", "acceptable_alternatives": ["bgpdd-plan"]}
{"prompt": "None of this is built yet and I don't even know what 'done' looks like for it. Can you help me write a spec first?", "expected_skill": "bgpdd-plan", "acceptable_alternatives": ["bgpdd-lite"]}
{"prompt": "The nightly reconciliation job silently drops rows sometimes. Find out why and patch it - this is small and contained, not a redesign.", "expected_skill": "bgpdd-bugfix", "acceptable_alternatives": []}
{"prompt": "Should Rex or Alex own writing the honing transcript step? I keep seeing them step on each other's turf in the pipeline docs.", "expected_skill": "agent-audit", "acceptable_alternatives": []}
{"prompt": "After that deploy went sideways last night I want to sit down and capture what went wrong so we don't repeat it next sprint.", "expected_skill": "bgpdd-learn", "acceptable_alternatives": []}
{"prompt": "We need full requirements, architecture, and a plan for a multi-tenant billing system - nothing like it exists in the codebase today.", "expected_skill": "bgpdd-plan", "acceptable_alternatives": []}
{"prompt": "This was supposed to be a two-line config change to bump a timeout value, but now three unrelated tests are failing - can you sort out what broke?", "expected_skill": "bgpdd-bugfix", "acceptable_alternatives": []}
{"prompt": "Map the existing billing APIs and reverse-engineer the legacy QA paths before we plan the enhancement.", "expected_skill": "bgpdd-discovery", "acceptable_alternatives": []}
{"prompt": "Brownfield: run global discovery on this repo so Iris/Scout/Echo fill .docs/summary before we touch the feature.", "expected_skill": "bgpdd-discovery", "acceptable_alternatives": []}
{"prompt": "Build is green and ship-decision says GO - run the Launch Squad and ship it.", "expected_skill": "bgpdd-shipping", "acceptable_alternatives": []}
{"prompt": "Launch the app: Vera, Cipher, Dep against the shipping checklist, then open the PR.", "expected_skill": "bgpdd-shipping", "acceptable_alternatives": []}
{"prompt": "Iris and Echo already mapped the login flow last sprint and Quinn baselined the locked-account behavior at a 423 with a specific error body - can you confirm the running service still matches that baseline? I don't want any code touched, just check it still holds.", "expected_skill": "bgpdd-verify", "acceptable_alternatives": []}
{"prompt": "searchAccounts is a bad name now that it takes a free-text query - rename it to searchAccountsByQuery in src/api/client.js and update the one caller in DashboardView.vue. Nothing else changes.", "expected_skill": "bgpdd-quick", "acceptable_alternatives": ["bgpdd-lite"]}
{"prompt": "searchAccounts happily fires a request when the query is undefined. Add a guard so it short-circuits on an empty query, and a test in tests/ that covers it.", "expected_skill": "bgpdd-quick", "acceptable_alternatives": ["bgpdd-bugfix"]}
{"prompt": "src/config.js has gone scruffy - stale comment about a bump that already happened, no doc comment on searchPageSize. Tidy that one file up, no behaviour change.", "expected_skill": "bgpdd-quick", "acceptable_alternatives": []}
{"prompt": "npm test is red: tests/reports.spec.js asserts the request timeout is 8000 and src/config.js says 30000. Work out which side is wrong and get it green.", "expected_skill": "bgpdd-bugfix", "acceptable_alternatives": []}
{"prompt": "Account search on the dashboard round-trips the API on every single keystroke and staging is falling over under it. Can you track that down and fix it?", "expected_skill": "bgpdd-bugfix", "acceptable_alternatives": ["bgpdd-lite"]}
{"prompt": "Server-side paging for /accounts/search: page and pageSize query params, same shape as /reports/export already uses, page size from config. The contract is settled, there's nothing left to design - just needs writing up properly and building.", "expected_skill": "bgpdd-lite", "acceptable_alternatives": ["bgpdd-plan"]}
{"prompt": "I want a second service that takes settlement webhooks from the payment provider and feeds the nightly reconciliation job. New schema, new contract with them, and I genuinely don't know yet what should happen when a webhook arrives twice.", "expected_skill": "bgpdd-plan", "acceptable_alternatives": ["bgpdd-discovery"]}
{"prompt": "/bg the Export CSV button on the reports page 500s whenever I leave the date range blank", "expected_skill": "bgpdd-bugfix", "acceptable_alternatives": ["bg"], "expected_chain": ["bg", "bgpdd-bugfix"]}
{"prompt": "/bg rename searchAccounts to searchAccountsByQuery across client.js and DashboardView.vue", "expected_skill": "bgpdd-quick", "acceptable_alternatives": ["bg"], "expected_chain": ["bg", "bgpdd-quick"]}
