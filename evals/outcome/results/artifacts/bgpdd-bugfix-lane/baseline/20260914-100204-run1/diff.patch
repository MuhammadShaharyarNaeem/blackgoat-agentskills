diff --git a/src/coupons.js b/src/coupons.js
index c461e0c..53c0928 100644
--- a/src/coupons.js
+++ b/src/coupons.js
@@ -28,6 +28,10 @@ class CouponError extends Error {
 }
 
 function applyCoupon(code) {
+  // The coupon field is optional: an order without one prices at full.
+  if (code === undefined || code === null) {
+    return { coupon: null, discountPercent: 0 };
+  }
   const normalized = code.toUpperCase();
   if (!COUPONS.has(normalized)) {
     throw new CouponError('unknown coupon code');
