{
  "version": 1,
  "description": "TEMPLATE, not auto-loaded. Cursor reads hooks from <project>/.cursor/hooks.json or ~/.cursor/hooks.json, not from a plugin's hooks/ folder: copy this file to one of those paths and replace <PLUGIN_ROOT> with this plugin's install root. Cursor's documented pre-execution hooks cover shell, MCP and file reads, so only rule 1 (commit through the gate) is mechanically enforceable there; rules 2-4 hold as prose contract under Cursor -- see rules/cursor-runtime.mdc.",
  "hooks": {
    "beforeShellExecution": [
      {
        "command": "python <PLUGIN_ROOT>/skills/pipeline-tools/scripts/guard_action.py --format cursor"
      }
    ]
  }
}
