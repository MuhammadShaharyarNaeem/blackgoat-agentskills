# Orders — Test Report

The substitution this gate exists to name: a capture that looks complete but was taken
from inside the process it claims to have observed from outside. The body even carries
the envelope — the in-process pipeline really does wrap it. What the tier cannot show
is whether the *hosted* app does.

#Task [1]:

**Runtime evidence:** evidence/runtime/m3-orders-post.md

- FR-4: PASS — Tests.Integration/Orders/EnvelopeTests.cs::Returns_envelope — exit 0 — 4 passed
